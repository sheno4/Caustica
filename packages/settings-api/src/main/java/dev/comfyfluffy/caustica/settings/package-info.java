/**
 * Typed feature options, current values, and renderer-independent display text.
 *
 * <p>Features use the same {@link ResourceId} for their engine and settings registrations. The settings
 * registry is independent of the renderer, and hosts are responsible for storing supported option kinds.
 */
package dev.comfyfluffy.caustica.settings;
