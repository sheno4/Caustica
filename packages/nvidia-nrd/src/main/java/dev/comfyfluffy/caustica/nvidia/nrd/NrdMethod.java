package dev.comfyfluffy.caustica.nvidia.nrd;

/** NRD diffuse/specular method instantiated by the native integration. */
public enum NrdMethod {
    RELAX_DIFFUSE_SPECULAR,
    REBLUR_DIFFUSE_SPECULAR
}
