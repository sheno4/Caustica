package dev.comfyfluffy.caustica.api.provider;

/** Where geometry obtains the base-color texture paired with a compiled material. */
public enum MaterialTextureKind {
    SHARED_ATLAS,
    STANDALONE
}
