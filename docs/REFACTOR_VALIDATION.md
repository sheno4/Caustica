# Refactor validation

The project-wide aesthetic review includes interactive behavior and visual inspection. Failures found by these checks are part of the refactor/fix work. Compilation and unit tests do not substitute for the interactive checks.

## Queued lifecycle checks

- Cross dimensions in one process: Overworld → Nether → Overworld → End → Overworld, then Nether → End directly. Wait for visible terrain and active RT at each arrival; check scene/sky/light replacement, exposure and temporal-history settling, stale geometry, crashes and cleanup. Repeat with RT disabled during travel and re-enabled after arrival; include portal travel when computer-use input is verified.

- Reload resources with RT active through F3+T or the resource-pack screen. Verify renderer detachment before atlas replacement and recovery after the reload future completes; repeat a reload to exercise generation ordering after the method-wrapper refactor.
- Enter the debug workbench, wait for active RT and visible geometry, exit to the title screen, enter a different existing test world in the same client process, and return to the first world. Check coherent scene replacement, camera/history reset, stale geometry, errors and shutdown.
- Toggle RT off/on five times in one world, waiting for the requested state and fresh rendered frames each time. Inspect vanilla terrain while disabled and world, hand, HUD and transparency after enabling. Repeat a toggle after switching worlds.
- Leave a world while RT preparation is active, then open another world. Check that completed jobs from the old world cannot publish into the new session.

## Queued interactive exploration

- Inspect the Nether's ember-lit ash dome from open terrain and above the roof, and the End's violet nebula and stars from the central island and open void. Turn through the entire dome, inspect horizons and reflections, and watch exposure after arrival. Check that neither dimension inherits Overworld celestial lights or sky textures. Repeat RT toggles and a resource reload in each dimension.

- Relaunch the rebuilt client after texture-binding accessor changes. Confirm successful mixin application and inspect textured entities, including their material maps, to verify primary texture resource lookup.
- Use computer use to navigate pause/title/world-selection menus and inspect the settings screen. Check opening/closing screens, UI placement, readable text, transparency and correct input restoration.
- Open a settings dropdown, press its row again to close it, and repeat using keyboard activation. Switch directly between two dropdowns; select an item and click outside the popup. Check that only one list stays open and row clicks do not immediately reopen it.
- Navigate to a stepped slider by keyboard, toggle editing with Enter, and use left/right arrows. Check that arrows adjust by one step only while editing is enabled, and that hidden or unavailable rows cannot change values.
- Change a setting prerequisite while keeping its page open. Check that affected toggles, sliders, dropdowns and group headers update availability for focus, input and appearance without rebuilding the screen; include disabling a slider during a drag and a dropdown while its popup is open.
- Inspect a stationary workbench camera, then move through terrain and look at cutout surfaces, emissive geometry, water/reflections and moving entities. Compare final output with relevant normal/depth/radiance views when a visual defect appears.
- Exercise window resizing and reconstruction-mode changes where available; inspect resolution transitions, stale targets, ghosting and exposure settling.
- Compare bloom enabled/disabled around bright emitters while exposure settles in bright and dark scenes. The exposed bloom pyramid must return to the scene's scale before composition; inspect highlight strength for an extra exposure-dependent gain.
- Inspect diffuse, metallic and thin-sheet materials from both sides at normal and grazing angles, including smooth and rough surfaces. Compare direct and indirect highlights, transmission and denoiser signals after the shared BSDF evaluator refactor. Include reflection/transmission splits that fill all three stable planes; check plane metadata, restored output and GPU timing after the branch-enqueue simplification.
- Verify the NGX RR diagnostic overlay's axis controls with a before/after capture. Do not alter world/UI orientation to compensate for the native indicator.
- After native integration changes, relaunch with the rebuilt libraries and exercise RR, SR, RELAX and REBLUR transitions at a repeatable camera. Inspect LUT presentation and temporal history; exercise frame generation where supported.

Use named debug operations for repeatable state checks and computer use for actual UI exploration. Save evidence under `tmp/` and `run/caustica-debug/`; restore changed settings, input state and views. Make checkpoint commits after verified coherent changes. Each run must record actual results and limits rather than marking a queued check complete from intent.

## Results: 2026-09-08

- Five RT off/on cycles completed in one workbench session using `tools/debug/check_rt_lifecycle.py`. Every transition reached the requested active/frame-active state; disabled checks waited for game ticks, enabled checks for RT frames. All ten screenshots were captured and the initial requested state restored. Evidence: `tmp/aesthetic-toggle-five.json`. Final off/on captures show vanilla terrain/hand/HUD and restored RT output. This stationary check does not cover all queued materials or world transitions.
- The NGX RR evaluation now sets `InIndicatorInvertYAxis = 1`. Native Release build and five NGX tests passed. Live captures confirm upright diagnostic text, including the repeatable workbench view in `tmp/aesthetic-rr-upright.json`.
- Computer use found the Minecraft window, but returned a black capture and failed activation twice with `GetCursorPos: Access is denied`. Menu navigation, world switching and UI exploration remain pending until desktop control works.
- Runtime logs contain authentication and invalid resource-pack path errors; no renderer exception was observed during the five-cycle run. Log: `tmp/aesthetic-lifecycle-runtime.log`.
- After removing the look-package loaders/schema, the rebuilt client reached active RT at frame 12701. Workbench capture at frame 22796 shows terrain, water reflections, hand/HUD and upright RR text; exposure reads -12.02 EV. Evidence: `tmp/aesthetic-hardcoded-look-{runtime.log,status.json,screenshot.json}`. Window dimensions differ from the earlier capture, so this is a visual smoke check, not a pixel comparison. RELAX/REBLUR/SR/FG transitions remain queued.
- RR -> RELAX/SR -> REBLUR/SR -> RR completed twice in the workbench with 240 RT frames per state and settings restored (`tmp/aesthetic-denoiser-transitions*.json`). Both NRD modes show world/reflections. SR's native diagnostic showed black bars; enabling its indicator Y inversion moves them to the expected orientation. Raw captures show the bars/glyphs already in reconstructed-color and absent from trace-color. Sampled glyph peak is 0.000360 at preExposure 0.000312 (`tmp/sr-indicator-buffers.json`). SR diagnostic legibility remains unresolved; do not mark this visual defect fixed. FG and HDR presentation remain untested live.
- After grouping per-scene CPU caches and removing redundant per-scene frame claims, five further RT off/on cycles completed with the initial requested state restored. Final off/on captures show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text; enabled capture frame 7977. Evidence: `tmp/aesthetic-scene-caches-toggles.json` and `tmp/aesthetic-scene-caches-runtime.log`. Client shutdown exited 0. The subsequent identity-set cleanup passed tests but was not included in this live build. This run does not cover menu navigation or world switching.
- After unifying frame instance records, simplifying light-index pages, completing feedback cleanup and sharing the adaptive-light shader scan, five more RT off/on cycles completed; all ten states settled and the initial requested state was restored. Final disabled/enabled screenshots at frames 12173/12465 show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text. Evidence: `tmp/aesthetic-scene-input-toggles.json`, `tmp/aesthetic-scene-input-runtime.log`. Client shutdown exited 0. Only known authentication/resource-pack errors were observed in this run's log. This is a stationary lifecycle/visual smoke check, not a performance or all-materials result.
- Computer Use rediscovered the newly launched window, but capture failed twice after refreshing the window binding: `IGraphicsCaptureItemInterop.CreateForMonitor failed: Could not capture the given monitor. (0x80070057)`. No UI input was sent. Menu/world-switch exploration remains pending.
- The integrated retained-scene build passed all project `test` tasks and five further RT off/on cycles. Final disabled/enabled captures at frames 11450/11748 show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text. Initial requested state was restored and client shutdown exited 0. Evidence: `tmp/aesthetic-retained-integration-tests.log`, `tmp/aesthetic-retained-integration-toggles.json`, `tmp/aesthetic-retained-integration-runtime.log`. Observed log errors were the known authentication/resource-pack errors. This stationary check does not verify world switching, interactive menus, or the corrected affine-volume normal calculation in a dedicated scene.
- Build `e76b26e3`, including the bloom, BSDF, branch scheduling and NRD ownership changes, completed five RT off/on cycles and two RR → RELAX/SR → REBLUR/SR sequences. The initial RT request, denoising settings and debug view were restored. Inspected captures show vanilla/RT terrain, water reflections, hand/HUD and upright RR text; SR's dark diagnostic bars remain unresolved. Evidence: `tmp/aesthetic-nrd-integrated-toggles.json`, `tmp/aesthetic-nrd-integrated-transitions.json`. Twelve raw buffers across the two NRD methods contain no non-finite values; nonzero plane-0 normal lengths range approximately 0.99931–1.00000 (`tmp/aesthetic-nrd-integrated-buffer-statistics.json`). These are stationary smoke checks, not all-material, bloom exposure, three-plane saturation or performance comparisons.
- The same run's Computer Use capture failed twice, including after refreshing the window binding, with `IGraphicsCaptureItemInterop.CreateForMonitor` / `0x80070057`. No UI input was sent; world switching and menu exploration remain pending. The client exited 0. Its log includes OS counter, stale Nsight layer-path, audio-device, authentication and resource-pack errors; no renderer exception was found during the exercised transitions (`tmp/aesthetic-nrd-integrated-runtime.log`).

## Rebuilt entity/material lifecycle check (7a27c4a7)

- Launched the debug workbench on 7a27c4a7; RT initialized and rendered. Five complete off/on cycles passed via check_rt_lifecycle.py, with active/frameActive transitions and frame/tick settling. Evidence: tmp/aesthetic-entity-live-toggles.json and tmp/aesthetic-entity-live-runtime.log. Final disabled frame 41623 and enabled capture frame 41898 were visually inspected; terrain, hand and HUD remain visible, and RR overlay text is upright. Client stopped gracefully and its Gradle process exited 0.
- Visual follow-up: the right-hand fluid column appears blue in the disabled screenshot but orange in enabled output. Inspect material assignment and upstream buffers before diagnosing; this pair alone is insufficient. Images: run/screenshots/caustica-debug-ac45a2e9-ace7-4641-8d28-fb0fb557f919.png and caustica-debug-a79b8437-21f6-4948-9add-6127be034db5.png.
- Computer-use Minecraft screenshots remained black after fresh window selection; activating the window failed with GetCursorPos Access denied (0x80070005). No gameplay input was issued. Enter/exit/different-world testing remains unverified. An earlier VS Code screenshot probe was rejected by automatic approval review as potentially exposing unrelated workspace content; continued with the Minecraft-only target instead.

## Fluid-column investigation (5dd22d10)

- Captured albedo, primary-depth and trace-radiance views in the unchanged workbench; restored view off. Evidence tmp/aesthetic-fluid-views.json. Orange column is present in trace-radiance, so RR reconstruction does not introduce it. Albedo shows ground through the water region. Primary-depth visualization was black and does not establish surface identity.
- Read-only server predicates found water at (-1,-58,9), (0,-58,9), (1,-58,9) within x[-6,9], z[3,23]. No lava was found in that plane/rectangle. A further narrow viewing-direction search through z60 also found no lava (tmp/aesthetic-fluid-world-scan.json, tmp/aesthetic-fluid-behind-scan.json). These bounded searches do not rule out lava elsewhere or prove an assignment bug.
- Inspected fluid tag selection and beginFace: water selects the logical water material and suppresses base texturing. Next inspect publication/shader program and primitive material indexing to identify the displayed column. No speculative source fix made. Debug client stopped cleanly after captures; no world blocks or player pose modified.

## Fluid reflection finding resolved

- Source trace preserves water identity through FluidTags.WATER, logical material selection, routing buckets, primitive record upload and water surface/volume binding. No assignment inconsistency found in these inspected paths.
- Read saved chunk palettes without modifying world files. Within chunks [-8,8] on each horizontal axis, found a lava wall at x=-9,z=-1..4 at camera height y=-58 (tmp/aesthetic-saved-lava-positions.json). A reflected ray off the front of the verified water column intersects this wall; the prior search tested the transmitted viewing direction and missed this side/back wall.
- Shifted the camera from (9.5,-60,2.5) toward z=-4, captured after 180 RT frames, then restored the exact original pose and captured after another 180 frames. Shifted image shows clear water and different reflected surroundings; restored image reproduces the orange stripe. Evidence tmp/aesthetic-fluid-reflection-views.json and run/screenshots/caustica-debug-849db7d5-0a93-4e6d-b0ab-3f8081937805.png / caustica-debug-2a8518d5-c9d3-4d47-9dd1-ab4c2fe4ea7b.png. Together with upstream trace and saved geometry, this supports reflected lava, not a material swap. No source fix required for this lead. World blocks unchanged, pose restored and client stopped gracefully.

- Rebuilt texture-accessor client (67667ed6) entered the workbench and completed five RT off/on cycles, restoring the initial requested state. Final disabled/enabled captures at frames 8646/8834 show terrain, textured hand and HUD, with upright RR diagnostics after re-enabling. No mixin or entity-texture lookup failure appeared in the inspected runtime log; known authentication, resource-pack filename, OSHI and Vulkan-layer warnings remain. Evidence: tmp/aesthetic-texture-accessor-toggles.json and tmp/aesthetic-texture-accessor-runtime.log. The client stopped gracefully, exit 0. This verifies the exercised runtime path, not all entity material maps, settings interactions, or world switching.

## Current-build lifecycle validation (29329b3b)

- Launched the checkpointed Fabric client into caustica-debug-workbench. Five RT off/on cycles passed and restored the initial requested state (tmp/aesthetic-final-rt-toggles.json). A further disabled/enabled capture pair shows vanilla terrain when disabled and world, hand, HUD and upright RR diagnostics when enabled. Evidence: tmp/aesthetic-final-toggle-captures.json, frames 56254/56430, and tmp/aesthetic-final-runtime.log. Client stopped gracefully, exit 0. This run used 854x480 output; it is not target-resolution performance evidence.
- Computer Use selected the returned Minecraft window but initially captured unrelated content. After one fresh selection and activation, its capture showed the Minecraft title with a blank white client area. No gameplay input was issued from either image. World transitions and settings interaction remain unverified; renderer captures were used only for visual inspection, not desktop input coordinates.

## Shipped binary asset verification (3711b635)

- Rebuilt the five SDR/HDR display-transform LUTs in tmp/aesthetic-lut-reproduction using the locked Python environment and pinned OCIO configuration. Every file matched its shipped binary byte-for-byte. All six shipped CLUT files, including the imported LMT, have the expected header/version/shaper range, 65-cubed RGBA16F payload, finite values and alpha 1. Evidence: tmp/aesthetic-lut-artifact-audit.json. The LMT source cube is not tracked, so this does not claim source reproduction for that table.
- The actual bundled GLB passed GltfViewerModelTest: hierarchy, meshes, material texture bindings and embedded-image assertions. Independently decoded its four embedded 2048x2048 PNGs after checking container length/chunk bounds. Evidence: tmp/aesthetic-bundled-model-check.log and tmp/aesthetic-model-images-audit.json. These checks establish loadability and content structure, not in-game appearance or author provenance.

## Target-resolution sample and unresolved device loss (015b2ee4)

- A temporary windowed launch requested3840x2160 but Windows clamped it to3840x2130. This run failed with vkQueueSubmit2(compute)=-4 at23:41:34; terrain preparation/publication errors propagated that device loss. Crash report: run/crash-reports/crash-2026-09-08_23.41.35-client.txt; runtime log: tmp/aesthetic-4k-runtime.log. Stop request lost its connection and the process exited1. This is a failed stability check.
- The diagnostic review found NVIDIA System event153 at23:41:23.341, before the application failure, without a decoded cause. VMA local usage was3300.51MiB against12796MiB budget; device-fault description/addresses/vendor records were empty. Neither these records nor source inspection establish whether the cause was shader work, synchronization, acceleration structures or the driver. No speculative fix made.
- A fullscreen repeat verified3840x2160 output,1920x1080 RR Performance input,32-section render distance and active/unpaused runtime. After120warmup frames it recorded600requested frames (610Frame events with recording boundaries). Frame scope mean18.72ms,p9519.13ms,p9919.64ms; GPU world resources/trace mean14.70ms, RR3.66ms. Nested scopes overlap and are not additive. This stationary workbench sample is not representative gameplay or proof of stability. Evidence: tmp/aesthetic-4k-measurement.json, tmp/aesthetic-4k-analysis.json and recording-512d78d7-f735-4f52-9d68-f045c0c32b7d.jfr.
- Fullscreen repeat stopped gracefully, exit0. Restored the original fullscreen:false preference; renderDistance32 unchanged. Exact-resolution timing progressed; initial device loss remains an open defect requiring reproduction/diagnosis.

## Device-loss diagnostic coverage

- Fault reporting now receives the graphics and compute queues from the live device context instead of remembering only the most recent compute submission. Minecraft's decoded checkpoint report has its own once-only guard, so an earlier compute-side fault report does not suppress it. Device-wide idle failures use the same diagnostic path.
- Plain gradlew.bat check passed across all 24 packages and Slang tooling (187 tasks, 42 seconds); evidence: tmp/aesthetic-device-diagnostics-check.log. These checks validate compilation and existing behavior, not a deliberately induced GPU fault. The observed 4K device loss remains unresolved.

## Windowed device-loss reproduction (130d9689)

- Repeated the same windowed request, observing 3840x2130 output and 1920x1065 RR Performance input. The workbench was ready, active and unpaused at frame 944. At 23:56:37, about 69 seconds after RT activation, vkQueueSubmit2(compute) reported device loss with latest graphics frame 3717. Both graphics and compute checkpoint queries ran and returned no markers. Evidence: tmp/aesthetic-device-loss-repeat-start.json and tmp/aesthetic-device-loss-repeat.log.
- This fault report contained 241 address records, capped by the reporter to 64: the first was WRITE_INVALID at address 0 with precision 0x1000; the following inspected records were instruction pointers (IP_UNKNOWN), not identified resource-buffer addresses. VMA usage remained 3300.51 MiB against 12796 MiB device-local budget. This supports an invalid GPU write near zero; it does not identify its originating shader, submission, or root cause.
- The client crashed (run/crash-reports/crash-2026-09-08_23.56.38-client.txt), and the Gradle process exited 1. The 2400-frame recording request failed when the service disconnected; no completed recording or timing result was produced. Subsequent high-resolution RT toggles were not run. fullscreen:false and renderDistance:32 remain unchanged. The defect is reproduced and unresolved; odd-resolution indexing is a lead to inspect, not an established cause.

- Follow-up fault reporting retains all driver-reported address/vendor records in native heap allocations rather than truncating to 64 stack records. Only read/write fault addresses are compared with registered buffers; instruction pointers are not presented as nearby buffer resources. Full root check passed (187 tasks, 41 seconds; tmp/aesthetic-full-fault-report-check.log). The larger report requires another actual fault to exercise.
- Independent bounded review found no odd-extent violation in inspected trace allocation/dispatch, NEE tile and reprojection bounds, or presentation/bloom/NRD output bounds. This weakens the simple padded-thread hypothesis without excluding other shader or resource-lifetime defects.

## Longer fullscreen run and 4K lifecycle checks (62a9c1eb)

- A stationary fullscreen run at 3840x2160 with RR Performance input 1920x1080 completed 6000 requested active frames, from frame 1022 to 7024 (ticks 469 to 2727). This interval alone spans approximately 113 seconds at the observed world tick rate, exceeding both prior windowed failure intervals. No device loss occurred in this run. Evidence: tmp/aesthetic-fullscreen-long-status.json and tmp/aesthetic-fullscreen-long-runtime.log. This supports continued investigation of window dimensions/presentation; it does not prove a resolution-dependent cause or general stability.
- Five complete RT off/on cycles then passed at 4K, restoring the initial requested enabled state. Final disabled frame 9037 and enabled capture frame 9083 were inspected: vanilla terrain is present when disabled; RT terrain, hand, HUD and upright RR overlay return when enabled. Evidence: tmp/aesthetic-4k-rt-toggles.json and screenshots caustica-debug-dc2b4cd2-71b1-401a-8466-c40784427278.png / caustica-debug-817f74e6-c341-4ed6-8561-f346936f4459.png. The full-size enabled capture exceeded the image-reader transport; tmp/aesthetic-4k-enabled-preview.png was generated locally for inspection.
- Client stopped gracefully with acknowledgement and Gradle exit 0. fullscreen:false and renderDistance:32 remain unchanged. These debug-service transitions and renderer captures do not establish computer-use menu interaction or cross-world behavior. The windowed device loss remains unresolved; a windowed extent with an aligned RR input height is the next controlled comparison.

## Windowed aligned-height comparison (c18d0bba)

- Requested and rendered 3840x2112 windowed output with RR Performance input 1920x1056. RT activated at 00:10:20 and device loss recurred at 00:10:59, latest graphics frame 2061. This rules out an odd or non-workgroup-aligned input height as a necessary condition for the observed fault; it does not isolate window presentation from all renderer/driver behavior.
- Complete driver report contains 208 records: one WRITE_INVALID at reported address 0, precision 0x1000, and 207 IP_UNKNOWN instruction addresses. Both queues returned no checkpoints. VMA device-local usage was 3291.81 MiB against 12796 MiB budget. Complete native report allocation was exercised successfully. Evidence: tmp/aesthetic-window-aligned-runtime.log and tmp/aesthetic-window-aligned-faults.json.
- Client crashed and Gradle exited 1; crash report run/crash-reports/crash-2026-09-09_00.11.01-client.txt. The planned 6000-frame observer started after the crash and failed on its initial status call, so it produced no interval result. fullscreen:false and renderDistance:32 remain unchanged.
- Fixed a separate observed measurement-tool issue: an unavailable JFR stop no longer prevents writing the experiment's initial conditions/revision. A behavioral disconnect regression and the existing debug-tool tests pass (6 tests; tmp/aesthetic-measure-failure-check.log). Partial reports omit unavailable completion/recording fields.

## RT-disabled windowed baseline (62bbcaa8)

- Ran the same workbench at 3840x2112 with a launch-only caustica.rt=false override. Status confirmed ready/unpaused and inactive RT throughout the observed interval, ticks 690 to 3091 (2400 requested ticks, approximately two minutes). Client stopped gracefully and Gradle exited 0. Evidence: tmp/aesthetic-window-vanilla-status.json, tmp/aesthetic-window-vanilla-runtime.log and tmp/aesthetic-window-vanilla-stop.json. fullscreen:false and renderDistance:32 remain unchanged; the launch override does not change the saved RT preference.
- The fault has reproduced in the RT-active windowed runs, but not this RT-disabled baseline. This narrows the investigation toward active renderer/reconstruction work and its interaction with presentation; it does not prove a specific component is at fault.
- Retried Computer Use against the freshly returned Minecraft window with RT disabled. Initial capture showed unrelated content; fresh app/window selection and activation then returned a blank white Minecraft client area. No gameplay input was issued. Menu/world-transition tests remain unverified, and the blank computer-use capture is not evidence of a blank game render target.

## Raw windowed RT comparison (9100be75)

- At 3840x2112 windowed output, launch-only denoising.route=raw completed 2400 requested world ticks (407 to 2808, frames 264 to 2462) with RT active. No device loss occurred. Raw mode bypasses RR/SR/NRD and traces at native resolution, so workload and temporal behavior differ from RR Performance; this does not independently identify RR as the fault source. Evidence: tmp/aesthetic-window-raw-status.json and tmp/aesthetic-window-raw-runtime.log.
- Post-interval capture frame 2739 shows world, hand and HUD with unreconstructed tracing noise; viewed through tmp/aesthetic-window-raw-preview.png. Original capture: run/screenshots/caustica-debug-fc1ed9a1-9050-47c1-a5f7-ceee844b2ffd.png. Client stop acknowledged, Gradle exited 0, fullscreen:false/renderDistance:32 unchanged. The route override was confined to this process.
- Source inspection found NRD compose pipeline creation was unconditional even on raw/RR routes. It is now created only for the temporal-denoiser route. Full root check passed, 187 tasks in 41 seconds (tmp/aesthetic-nrd-compose-lazy-check.log). This cleanup was made while the prior build was running and is not presented as a crash fix or as exercised by that run.

## Matched NRD/SR and validated RR comparisons (fd1a153d)

- NRD Reblur plus DLSS SR Performance used 1920x1056 input and 3840x2112 windowed output. The 2400-tick observation completed (ticks 622 to 3024, frames 1204 to 6561) with no device loss. Capture frame 8395 shows reconstructed world, hand and HUD; viewed via tmp/aesthetic-window-nrd-preview.png. Original screenshot: caustica-debug-a06f0fbd-0723-49ad-be88-92d77f23f15a.png. Client stopped gracefully, Gradle exited 0. Evidence: tmp/aesthetic-window-nrd-status.json and tmp/aesthetic-window-nrd-runtime.log. This also exercises temporal-only NRD compose creation from fd1a153d.
- Added development option -PvulkanValidation=true, forwarding Minecraft's --vulkanValidation argument. Actual RR launch requested VK_LAYER_KHRONOS_validation (installed version 1.4.357) and startup confirmed it enabled. Full root check passed (187 tasks, 10 seconds; tmp/aesthetic-validation-option-check.log). The shared client argument list serves both loader configurations; this live launch tested Fabric.
- With validation enabled, RR Performance at 1920x1056 -> 3840x2112 completed 2400 ticks (586 to 2987, frames 1189 to 6963) without a device loss or logged VUID/Validation Error. Client stopped gracefully, Gradle exited 0. Evidence: tmp/aesthetic-rr-validation-status.json and tmp/aesthetic-rr-validation-runtime.log. Validation overhead and the recent unused-pipeline cleanup both differ from the earlier failing runs; neither is established as the explanation. Repeat unvalidated RR on the current code before changing presets or attributing a fix.
- All route/method/quality overrides were launch-only; fullscreen:false and renderDistance:32 remain unchanged. No new computer-use interaction or cross-world claim follows from these renderer runs.

## Current RR reproduction and renewed computer-use probe (7872aacb)

- The unvalidated aligned-window RR run still failed after about 98 active seconds (graphics retirement `vkGetSemaphoreCounterValue`, frame 4565; 235 fault addresses, first WRITE_INVALID at zero). The unused NRD pipeline cleanup is not a fix for this fault. Evidence: `tmp/aesthetic-rr-current-runtime.log`, `tmp/aesthetic-rr-current-status.json`, crash `crash-2026-09-09_00.44.38-client.txt`.
- A fresh 854×480 workbench client produced an actual Minecraft computer-use capture after fresh window selection and activation. The subsequent Escape attempt returned “foreground window did not report a process id”; fresh inventory found no Minecraft window. The client independently logged compute-submit device loss at 00:51:10 and exited 1. Capture availability improved; interactive input remains unverified. Evidence: `tmp/aesthetic-cua-renewed-runtime.log`, crash `crash-2026-09-09_00.51.11-client.txt`.

## Computer-use recovery and dimension-transition regression

- Computer use on b766670e successfully captured gameplay, opened the pause menu with Escape, and returned to gameplay with Escape. Menu rendering and keyboard input are usable. The complete exploration matrix remains queued; this is a two-action smoke check.
- The first Overworld → Nether hop on b766670e failed with `no retained frame is available for UI recording`. The active runtime was presenting vanilla during the resource boundary, but the before-hand hook called RT UI recording despite an unsuccessful world composite. The host seam now uses the composite result to decide whether a retained frame exists for UI work. Full root checks passed after this change (187 tasks, 41 seconds); live repetition follows. Evidence: `tmp/aesthetic-dimensions-runtime.log`, crash `crash-2026-09-09_00.58.36-client.txt`, `tmp/aesthetic-dimension-ui-fix-check.log`.
- The UI fix allowed the Nether sky to render (frame 9372, `tmp/aesthetic-dimension-nether-roof.json`); inspected capture shows dark layered ash and an orange horizon. Nether → Overworld then exposed an independent empty-scene bug: a null hit table was dereferenced by ray dispatch while terrain was being rebuilt. Dispatch now emits an empty Vulkan hit region for an empty scene, with a regression test covering empty and populated regions. Evidence: `tmp/aesthetic-dimensions-fixed-runtime.log`.

## Cross-dimension retest (37863a2c)

- Full root checks passed with the empty-scene hit-region regression (187 tasks, 45 seconds). The live REBLUR/SR client then completed Overworld → Nether → Overworld → End → Overworld with active RT after each arrival. Inspected captures show the Nether ash/ember dome, End violet-blue nebula and stars, and restored Overworld workbench geometry/sky. Both previously reproduced Java crashes are absent from this repetition. Evidence: `tmp/aesthetic-dimension-retest-{nether,overworld,end}.json`, `tmp/aesthetic-dimension-end-{island,return}.json`, `tmp/aesthetic-dimensions-retest-runtime.log`.
- With RT disabled before each hop, Overworld → Nether → End → Overworld completed; RT was re-enabled and settled at every destination. All three disabled/enabled states and captures are in `tmp/aesthetic-dimension-toggles.json`. Original Overworld coordinates, yaw/pitch, creative mode, flying=false, flying speed and requested RT state were restored (`tmp/aesthetic-dimensions-restored.json`). Reconstruction settings were process-only REBLUR/SR overrides.
- Computer-use menu input is verified; full settings exploration, same-process world switching, portal-triggered travel, in-dimension resource reloads, full-dome/reflection inspection and RR cross-dimension travel remain queued. These checks do not establish a fix for the independent RR device loss or SR debug-overlay legibility.
- Graceful client shutdown exited 0 (5m31s total run). No RT composite exception or device-loss report occurred in the successful retest.

## Same-process world and settings exploration (c88aef18)

- Computer use navigated workbench → title → caustica-trace-rewrite-20260905 → title → workbench in one REBLUR/SR client. Both entered worlds settled to active/frame-active RT (second world frame 24308, return frame 49274). Inspected screenshots show each camera's geometry and the restored workbench hand/HUD. No RT composite exception, device loss or presentation failure latch appeared. Evidence: `tmp/aesthetic-world-switch-{runtime.log,second-status.json,return-status.json}`.
- Five further RT off/on cycles after the round trip passed, with ten captures and original requested state restored; final enabled frame 71374. Evidence: `tmp/aesthetic-world-switch-toggles.{json,log}`. Final computer-use capture shows the restored RT scene, hand and HUD.
- Pause, title, world selection, Options, Video Settings and Caustica Options navigation worked. The DLSS quality dropdown opened/closed by clicking its row, then opened/closed with two Enter presses. No setting values were changed. Done/Escape returned through the screen hierarchy to gameplay. Dynamic availability, sliders, switching between dropdowns and outside-click dismissal remain unverified.
- The F3+T tool chord only visibly toggled the debug display; no resource reload was logged. The display was restored with F3. Resource reload remains queued and must use a verified trigger.
- Original workbench runtime state was restored and graceful shutdown exited 0 (7m11s total client run). This run validates REBLUR/SR world switching and UI behavior; RR stability and the broader visual matrix remain open.

## Resource-pack reload failure and ownership fix (b696cb1f → ce83ca33)

- Computer use disabled the original SPBR-21 pack and applied the selection. Minecraft logged the reload at 01:20:55; default-pack RT recovered (frame 37751), and gameplay/capture showed the changed texture appearance. Restoring SPBR-21 triggered a second real reload at 01:22:17, which crashed with `Terrain preparation/publication failed`: the worker requested `minecraft:block/redstone_dust_line1_0` from a lookup without that material. Evidence: `tmp/aesthetic-resource-reload-runtime.log`, `tmp/aesthetic-reload-default-{status,capture}.json`, crash `crash-2026-09-09_01.22.20-client.txt`.
- Inspection found that reload start only reset rendering history; terrain and atlas borrowers remained attached. The fix closes/drains world resources synchronously before Minecraft replaces its images/model sets, and prevents the runtime tick from reopening a world while the resource-pack epoch is pending. Full root checks passed (187 tasks, 41 seconds), checkpoint ce83ca33.
- The rebuilt client reached active RT with SPBR restored. Renderer capture frame 22970 shows the workbench, textured hand/HUD and RT output (`tmp/aesthetic-reload-fixed-{status,capture}.json`). Computer-use capture returned a white client area, including after fresh selection and activation; no further UI input was sent. The repeated pack-swap regression remains unverified on the fix. This startup smoke check does not establish that the reload defect is resolved live.
- Rebuilt smoke-test client shut down cleanly (exit 0, 1m49s total run); the original SPBR selection is restored.
