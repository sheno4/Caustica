# Refactor validation

The project-wide aesthetic review includes interactive behavior and visual inspection. Failures found by these checks are part of the refactor/fix work. Compilation and unit tests do not substitute for the interactive checks.

## Queued lifecycle checks

- Enter the debug workbench, wait for active RT and visible geometry, exit to the title screen, enter a different existing test world in the same client process, and return to the first world. Check coherent scene replacement, camera/history reset, stale geometry, errors and shutdown.
- Toggle RT off/on five times in one world, waiting for the requested state and fresh rendered frames each time. Inspect vanilla terrain while disabled and world, hand, HUD and transparency after enabling. Repeat a toggle after switching worlds.
- Leave a world while RT preparation is active, then open another world. Check that completed jobs from the old world cannot publish into the new session.

## Queued interactive exploration

- Use computer use to navigate pause/title/world-selection menus and inspect the settings screen. Check opening/closing screens, UI placement, readable text, transparency and correct input restoration.
- Inspect a stationary workbench camera, then move through terrain and look at cutout surfaces, emissive geometry, water/reflections and moving entities. Compare final output with relevant normal/depth/radiance views when a visual defect appears.
- Exercise window resizing and reconstruction-mode changes where available; inspect resolution transitions, stale targets, ghosting and exposure settling.
- Compare bloom enabled/disabled around bright emitters while exposure settles in bright and dark scenes. The exposed bloom pyramid must return to the scene's scale before composition; inspect highlight strength for an extra exposure-dependent gain.
- Inspect diffuse, metallic and thin-sheet materials from both sides at normal and grazing angles, including smooth and rough surfaces. Compare direct and indirect highlights, transmission and denoiser signals after the shared BSDF evaluator refactor. Include reflection/transmission splits that fill all three stable planes; check plane metadata, restored output and GPU timing after the branch-enqueue simplification.
- Verify the NGX RR diagnostic overlay's axis controls with a before/after capture. Do not alter world/UI orientation to compensate for the native indicator.
- After native integration changes, relaunch with the rebuilt libraries and exercise RR, SR, RELAX and REBLUR transitions at a repeatable camera. Inspect LUT presentation and temporal history; exercise frame generation where supported.

Use named debug operations for repeatable state checks and computer use for actual UI exploration. Save evidence under `tmp/` and `run/caustica-debug/`; restore changed settings, input state and views. Make checkpoint commits after verified coherent changes. Each run must record actual results and limits rather than marking a queued check complete from intent.

## Results: 2026-09-08

- Five RT off/on cycles completed in one workbench session using `tools/debug/check_rt_lifecycle.py`. Every transition reached the requested active/frame-active state; disabled checks waited for game ticks, enabled checks for RT frames. All ten screenshots were captured and the initial requested state restored. Evidence: `tmp/aesthetic-toggle-five.json`. Final off/on captures show vanilla terrain/hand/HUD and restored RT output. This stationary check does not cover all queued materials or world transitions.
- The NGX RR evaluation now sets `InIndicatorInvertYAxis = 1`. Native Release build and five NGX tests passed. Live captures confirm upright diagnostic text, including the repeatable workbench view in `tmp/aesthetic-rr-upright.json`.
- Computer use found the Minecraft window, but returned a black capture and failed activation twice with `GetCursorPos: Access is denied`. Menu navigation, world switching and UI exploration remain pending until desktop control works.
- Runtime logs contain authentication and invalid resource-pack path errors; no renderer exception was observed during the five-cycle run. Log: `tmp/aesthetic-lifecycle-runtime.log`.
- After removing the look-package loaders/schema, the rebuilt client reached active RT at frame 12701. Workbench capture at frame 22796 shows terrain, water reflections, hand/HUD and upright RR text; exposure reads -12.02 EV. Evidence: `tmp/aesthetic-hardcoded-look-{runtime.log,status.json,screenshot.json}`. Window dimensions differ from the earlier capture, so this is a visual smoke check, not a pixel comparison. RELAX/REBLUR/SR/FG transitions remain queued.
- RR -> RELAX/SR -> REBLUR/SR -> RR completed twice in the workbench with 240 RT frames per state and settings restored (`tmp/aesthetic-denoiser-transitions*.json`). Both NRD modes show world/reflections. SR's native diagnostic showed black bars; enabling its indicator Y inversion moves them to the expected orientation. Raw captures show the bars/glyphs already in reconstructed-color and absent from trace-color. Sampled glyph peak is 0.000360 at preExposure 0.000312 (`tmp/sr-indicator-buffers.json`). SR diagnostic legibility remains unresolved; do not mark this visual defect fixed. FG and HDR presentation remain untested live.
- After grouping per-scene CPU caches and removing redundant per-scene frame claims, five further RT off/on cycles completed with the initial requested state restored. Final off/on captures show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text; enabled capture frame 7977. Evidence: `tmp/aesthetic-scene-caches-toggles.json` and `tmp/aesthetic-scene-caches-runtime.log`. Client shutdown exited 0. The subsequent identity-set cleanup passed tests but was not included in this live build. This run does not cover menu navigation or world switching.
- After unifying frame instance records, simplifying light-index pages, completing feedback cleanup and sharing the adaptive-light shader scan, five more RT off/on cycles completed; all ten states settled and the initial requested state was restored. Final disabled/enabled screenshots at frames 12173/12465 show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text. Evidence: `tmp/aesthetic-scene-input-toggles.json`, `tmp/aesthetic-scene-input-runtime.log`. Client shutdown exited 0. Only known authentication/resource-pack errors were observed in this run's log. This is a stationary lifecycle/visual smoke check, not a performance or all-materials result.
- Computer Use rediscovered the newly launched window, but capture failed twice after refreshing the window binding: `IGraphicsCaptureItemInterop.CreateForMonitor failed: Could not capture the given monitor. (0x80070057)`. No UI input was sent. Menu/world-switch exploration remains pending.
- The integrated retained-scene build passed all project `test` tasks and five further RT off/on cycles. Final disabled/enabled captures at frames 11450/11748 show vanilla terrain/hand/HUD and restored RT geometry/reflections with upright RR text. Initial requested state was restored and client shutdown exited 0. Evidence: `tmp/aesthetic-retained-integration-tests.log`, `tmp/aesthetic-retained-integration-toggles.json`, `tmp/aesthetic-retained-integration-runtime.log`. Observed log errors were the known authentication/resource-pack errors. This stationary check does not verify world switching, interactive menus, or the corrected affine-volume normal calculation in a dedicated scene.
- Build `e76b26e3`, including the bloom, BSDF, branch scheduling and NRD ownership changes, completed five RT off/on cycles and two RR → RELAX/SR → REBLUR/SR sequences. The initial RT request, denoising settings and debug view were restored. Inspected captures show vanilla/RT terrain, water reflections, hand/HUD and upright RR text; SR's dark diagnostic bars remain unresolved. Evidence: `tmp/aesthetic-nrd-integrated-toggles.json`, `tmp/aesthetic-nrd-integrated-transitions.json`. Twelve raw buffers across the two NRD methods contain no non-finite values; nonzero plane-0 normal lengths range approximately 0.99931–1.00000 (`tmp/aesthetic-nrd-integrated-buffer-statistics.json`). These are stationary smoke checks, not all-material, bloom exposure, three-plane saturation or performance comparisons.
- The same run's Computer Use capture failed twice, including after refreshing the window binding, with `IGraphicsCaptureItemInterop.CreateForMonitor` / `0x80070057`. No UI input was sent; world switching and menu exploration remain pending. The client exited 0. Its log includes OS counter, stale Nsight layer-path, audio-device, authentication and resource-pack errors; no renderer exception was found during the exercised transitions (`tmp/aesthetic-nrd-integrated-runtime.log`).

## Rebuilt entity/material lifecycle check (7a27c4a7)

- Launched the debug workbench on 7a27c4a7; RT initialized and rendered. Five complete off/on cycles passed via check_rt_lifecycle.py, with active/frameActive transitions and frame/tick settling. Evidence: tmp/aesthetic-entity-live-toggles.json and tmp/aesthetic-entity-live-runtime.log. Final disabled frame 41623 and enabled capture frame 41898 were visually inspected; terrain, hand and HUD remain visible, and RR overlay text is upright. Client stopped gracefully and its Gradle process exited 0.
- Visual follow-up: the right-hand fluid column appears blue in the disabled screenshot but orange in enabled output. Inspect material assignment and upstream buffers before diagnosing; this pair alone is insufficient. Images: run/screenshots/caustica-debug-ac45a2e9-ace7-4641-8d28-fb0fb557f919.png and caustica-debug-a79b8437-21f6-4948-9add-6127be034db5.png.
- Computer-use Minecraft screenshots remained black after fresh window selection; activating the window failed with GetCursorPos Access denied (0x80070005). No gameplay input was issued. Enter/exit/different-world testing remains unverified. An earlier VS Code screenshot probe was rejected by automatic approval review as potentially exposing unrelated workspace content; continued with the Minecraft-only target instead.

## Fluid-column investigation (5dd22d10)

- Captured albedo, primary-depth and trace-radiance views in the unchanged workbench; restored view off. Evidence tmp/aesthetic-fluid-views.json. Orange column is present in trace-radiance, so RR reconstruction does not introduce it. Albedo shows ground through the water region. Primary-depth visualization was black and does not establish surface identity.
- Read-only server predicates found water at (-1,-58,9), (0,-58,9), (1,-58,9) within x[-6,9], z[3,23]. No lava was found in that plane/rectangle. A further narrow viewing-direction search through z60 also found no lava (tmp/aesthetic-fluid-world-scan.json, tmp/aesthetic-fluid-behind-scan.json). These bounded searches do not rule out lava elsewhere or prove an assignment bug.
- Inspected fluid tag selection and beginFace: water selects the logical water material and suppresses base texturing. Next inspect publication/shader program and primitive material indexing to identify the displayed column. No speculative source fix made. Debug client stopped cleanly after captures; no world blocks or player pose modified.
