# GPU crash investigation — 2026-09-12

Three distinct failures are tracked here: the original uninstrumented GPU null write remains unresolved; the earlier native host crash inside Khronos Validation Layer is separate; and the newer GPU-AV null read is exactly mapped to injected BDA validation code, with its upstream cause still unknown. See the [concise local BDA report](../tmp/retained-validation-split/BUG_REPORT.md) for evidence and limits. None establishes a cause or fix for the original null write.

## Bounded follow-up — 2026-09-13

A host observer reproduced the full-game BDA fault in PID 16504 and recorded populated validation inputs: all 21 observed reserved pushes matched nonzero slot-6 addresses, and four observed 8-byte copy sources contained nonzero BDA table pointers. Aftermath maps the new shader hash `4F72820EFB3F0BA2`, PC `0x60`, to the injected cached-range-end read at SPIR-V row 36573. This proves the intended host values, not copy completion or shader-time contents. [Host values and mapped fault](../tmp/bda-heap-repro/RUN_FINDING.md).

The reduced descriptor-heap probe then completed three correctly read-back submissions in each of three cases: shader instrumentation off with raygen (PID 27560), BDA-instrumented raygen (PID 56760), and BDA-instrumented compute (PID 57460). Both instrumented cases reported two BDA access injections and one error-logging injection. There was no Vulkan validation error or device loss in these bounded runs. The probe has no scene, traversal, DLSS, WSI, concurrent submissions, or early resource retirement, so success does not clear those full-game paths. [Control run](../tmp/bda-heap-repro/headless-v2/headless-runs/off-raygen-20260913-012028-924/stdout.log), [raygen run](../tmp/bda-heap-repro/headless-v2/headless-runs/bda-raygen-20260913-012044-554/stdout.log), [compute run](../tmp/bda-heap-repro/headless-v2/headless-runs/bda-compute-20260913-012113-576/stdout.log).

The first reduced control stopped before dispatch because two stale Nsight 2026.2 manifest paths produced loader errors. V2 preserves those exact messages and classifies them separately from Vulkan validation errors; unknown loader errors, VUIDs, GPU-AV errors and failed Vulkan results remain fatal. No registry, installed layer or global setting was changed. [Probe revision and evidence](../tmp/bda-heap-repro/headless-v2/README.md).

## Original application fault

### Faster cave renderer reproductions

Two additional no-validation cave runs reproduced the same null GPU write with the **original frozen engine pools**:

| Time | Renderer | Recorded frame | Aftermath evidence |
| --- | --- | ---: | --- |
| 15:54:31 | Compact 28-byte radiance intersection, repeat | 6397 | [Dump](../tmp/gpu-investigation/aftermath-dumps/java-41736-20260912_155431.nv-gpudmp), [decode](../tmp/compact-payload/aftermath-repeat-crash.json), [log](../tmp/compact-payload/cave-repeat.log). `WRITE_INVALID 0x0` without active shader warps. Nsight's Crash Info shows all eight reported GPU units idle and a not-started marker `0x2cd52`. |
| 16:00:29 | Direct SER hit-object extraction | 5588 | [Dump](../tmp/gpu-investigation/aftermath-dumps/java-42728-20260912_160029.nv-gpudmp), [decode](../tmp/ser-direct-compact/aftermath-crash.json), [log](../tmp/ser-direct-compact/cave.log). `WRITE_INVALID 0x0` plus 104 `IP_UNKNOWN` addresses and active shader warps. |

Both first report device loss at compute `vkQueueSubmit2`. Both dumps decode as `Error_DMA_PageFault`, with an engine reset, no adapter reset and no page-fault resource record. The completed timing recordings precede the faults by approximately 49 and 13 seconds respectively; that timing does not establish a JFR-stop cause. Timing-query pools remain cached after JFR stops and are recycled only by graphics-completion callbacks. The first of these new dumps was opened and inspected in Nsight's UI.

A subsequent direct-SER client launched through Nsight survived warmup, five-frame capture and graceful shutdown. Intermittent survival does not establish a fix. The original-versus-reused pool particle controls below likewise do not establish an application GPU crash fix.

The exact frozen engine and host encoder audit found no steady-state pool reset before GPU completion, timeline off-by-one, or premature query reuse. That GPU-completion finding does not exclude the potential original-pool submit-return race described below: completion can precede return from the native host submission. The underlying host implementation discards graphics submit and command-pool reset return values; the integrated diagnostics below now check both. Conditional off-thread device-idle/deferred-submit hazards remain unproven at crash time.

At **16:37:18**, the v3 CPU telemetry candidate with the blocker renderer and original engine pools also reproduced the null GPU write during its recording. [Dump](../tmp/gpu-investigation/aftermath-dumps/java-56516-20260912_163718.nv-gpudmp), [decode](../tmp/gpu-investigation/aftermath-cpu-v3-crash.json), [log](../tmp/gpu-investigation/cave-cpu-submit-v3.log). Compute submission first reported device loss; Aftermath again identifies `Error_DMA_PageFault` without a page-fault resource record. This incomplete timing run is excluded from CPU target assessment. A subsequent combined v3/SER/pooled-engine recording completed and shut down normally, providing bounded survival rather than proof of a crash fix.

### Native result checks and bounded host evidence

The integrated diagnostic candidate checks the actual host `vkQueueSubmit2KHR` result and stops after a failed `vkResetCommandPool` before the host rewinds its allocator. A fixed-capacity history keeps 4,096 submission, timeline, pool and idle/drain events plus 64 retained query anomalies independently of JFR. Normal recording uses fixed arrays; failure snapshots allocate only when collected. The history does not add GPU waits or change scheduling. [Candidate and hashes](../tmp/gpu-investigation/crash-diagnostics-candidate/ready.json).

Its live cave run applied both mixins, reached **23,191 frames / 10,165 client ticks**, and shut down with exit code 0 without a new dump or native failure. [Final status](../tmp/gpu-investigation/diagnostics-soak-status-final.json), [log](../tmp/gpu-investigation/cave-crash-diagnostics.log). World ticks were frozen; client tick counters describe runtime callbacks. Full engine/runtime/renderer checks and native shader validation passed, and Fabric's full client check passed 258 tests with NeoForge compilation also passing. This is bounded survival with pooled command buffers, not an established GPU crash fix.

The inline-query version subsequently completed **19,410 frames / 8,211 client ticks** and exited normally. [Final status](../tmp/shadow-query-diagnostics/soak-final.json), [log](../tmp/shadow-query-diagnostics/cave.log).

### Host submit-return lifetime condition

The original graphics cleanup has a potential host-lifetime race: a retirement worker can observe GPU completion and destroy a submitted command buffer's pool while the render thread is still inside the corresponding native submit call. Vulkan's object lifetime rules allow implementation access throughout the host command, whose duration ends only on return; device completion alone does not remove this obligation. [Object lifetime and command duration](https://docs.vulkan.org/spec/latest/chapters/fundamentals.html#fundamentals-objectmodel-lifetime), [submission access](https://docs.vulkan.org/refpages/latest/refpages/source/vkQueueSubmit2.html).

Pool reuse avoids this destruction path: the worker returns a Java lease, and native reset occurs on the render thread during later acquisition, after submit has returned. Normal compute submission/wait/retirement already run sequentially on the same worker. The race is a code-level possibility, not a captured explanation for the DMA fault. A history sequence around native calls alone cannot prove overlap because the render thread may be descheduled after native return but before recording its return marker. An original-pool control gated on both GPU completion and host submit return would distinguish this lifetime condition from reduced native destruction frequency.

This direct-command-buffer concern does not establish a blanket host-submit lifetime extension for every buffer, image or query pool referenced by recorded commands. The recorded-resource access rules explicitly cover recording and pending execution; no remaining violation for those resources was established solely from retirement preceding host submit return. Gating all graphics retirement on both GPU completion and the corresponding successful native submit return remains a documented diagnostic-control idea, not an implemented production change or an established crash fix.

### Aftermath capture at 14:36

The immutable shadow-payload renderer reproduced the original signature during the copied glass fixture run: [log](../tmp/gpu-investigation/glass-shadow-payload.log), latest recorded frame **7163**, `WRITE_INVALID 0x0` and 71 `IP_UNKNOWN` addresses. The first diagnostic report came from graphics retirement's `vkGetSemaphoreCounterValue`. No checkpoint override was enabled in this run. The preceding fixture captures completed successfully; the player remained at the white-glass reflection view.

Aftermath produced [the GPU crash dump](../tmp/gpu-investigation/aftermath-dumps/java-20368-20260912_143605.nv-gpudmp) and eight `.nvdbg` files. [Decoded JSON](../tmp/gpu-investigation/aftermath-shadow-crash.json) reports `Error_DMA_PageFault`, an engine reset without an adapter reset, and no decoded page-fault resource record. Nsight's exception summary reports an MMU fault and an internal command-processing error. That wording permits driver or application command-recording/synchronization causes; it does not distinguish them. Active ray-tracing warps and their instruction locations are captured, but do not by themselves identify the instruction that caused the fault.

`-Dcaustica.debug.shader-dump-directory=<directory>` exports each exact compiled world-stage SPIR-V binary once per compiler cache entry, with unique filenames. It is optional and performs no per-frame work. These binaries let Aftermath resolve shader hashes against the actual application compiler output.

Recompiling the retained crash-time sources through the same native Slang API matched all three application shader hashes exactly: `770CF4A96B6A6B7B` is SER stable-plane fill, `2B74BCD8CF8B12A3` is shadow closest-hit, and `F3B957A5CC18AE61` is radiance closest-hit. All eight compiled stage sizes match the crash log. The [mapped decode](../tmp/aftermath-map/aftermath-shadow-crash-mapped.json) resolves 57 of 78 PC rows to Slang source locations; [mapping evidence](../tmp/aftermath-map/mapping-summary.json) preserves input hashes. These are active instruction locations, not a reported faulting instruction. The dump still has no page-fault resource details.

On RTX 5070 Ti, NVIDIA driver 616.56, the original run rendered DLSS Ray Reconstruction Performance at 1920×1027 → 3840×2054. At 12:18:43, terrain preparation failed because `vkGetQueryPoolResults(BLAS compaction)` returned `VK_NOT_READY` (1). At 12:18:44, `vkQueueSubmit2(compute)` reported device loss. Device-fault diagnostics included `WRITE_INVALID`, address `0x0`, precision `0x1000`, with no resolved buffer and no NVIDIA checkpoints.

The compaction query is read after the compute timeline wait. The observed ordering does not establish that compaction caused the GPU fault; `VK_NOT_READY` may be an early symptom. The audited build sizes, micromap triangle indexing, resource retention, and compute-to-graphics dependencies yielded no confirmed explanation for the null GPU write.

Evidence: [original-crash.log](../tmp/gpu-investigation/original-crash.log).

### Fresh reproduction and OMM control

The application fault reproduced without validation in a copied ray-tracing test world at 1280×720, using the same baseline build artifacts:

- **OMM on:** [glass-current.log](../tmp/gpu-investigation/glass-current.log), 13:07:51, graphics frame 8817. `vkQueueSubmit2(compute)` reported device loss; device-fault data contained only `WRITE_INVALID`, address `0x0`, precision `0x1000`. Heap 0 usage was 1588.02 MiB, with 3887 live BDA buffers totaling 257.80 MiB. [Crash report](../run/crash-reports/crash-2026-09-12_13.07.52-client.txt).
- **OMM off:** [glass-omm-off.log](../tmp/gpu-investigation/glass-omm-off.log) confirms `Opacity micromap acceleration: false` at startup, followed by the same device fault at **13:09:34**, graphics frame **13037**. Heap 0 usage was again 1588.02 MiB. The property preceded the Java argument file; startup confirms the effective setting.

OMM is not necessary for this reproduction, and high VRAM usage is not necessary either. Neither fresh sequence requires a preceding compaction-query `VK_NOT_READY` error. The first failing API is the compute submission, but that does not identify which queue's prior GPU commands caused the fault. Both queues still reported no diagnostic checkpoints in these runs. The application crash remains unresolved.

`-Dcaustica.debug.gpu-checkpoints=true` records NVIDIA checkpoints at renderer debug-scope and command-stage start/end boundaries when supported, including NGX evaluation and post-fill barriers. Device-loss output resolves opaque numeric tokens to the boundary, label, and command-buffer address. A 16,384-entry ring bounds retained history; overwritten or foreign tokens are reported as unknown/expired and are never dereferenced. Fault-time logging includes up to 33 neighboring recorded tokens from the same command to expose enclosing stages; those neighbors do not establish GPU execution progress. The option defaults off. Three focused scope-ordering, ring-expiration, and neighborhood tests passed; engine and runtime jars compiled.

### Checkpoint and feature controls

All three runs below reproduced `WRITE_INVALID` at address `0x0`:

| Run | Graphics checkpoint evidence | Interpretation |
| --- | --- | --- |
| [RR with checkpoints](../tmp/gpu-investigation/glass-checkpoints.log), 13:16:12, frame 10685 | TOP and BOTTOM: END `RT fill stable planes`, token `0x7d130`, command `0x2209db7d8d0`; compute reported none. | The last reported marker follows fill; the then-unmarked reconstruction/finish gap was not localized. |
| [RAW control](../tmp/gpu-investigation/glass-raw-control.log), 13:18:22, frame 7812 | TOP: END `RT trace rays` (`0x5b263`); BOTTOM: START (`0x5b262`), command `0x2ca37aef8d0`. Both queue queries returned these same markers. | Output was 640×360 to retain the RR run's 640×360 trace resolution. RR is not necessary for the fault. The generic nested trace label does not distinguish build from fill. |
| [SER off](../tmp/gpu-investigation/glass-ser-off.log), 13:20:42, frame 4860 | TOP and BOTTOM: START `RT post chain`, token `0x48a9e`, command `0x293e8a960d0`; compute reported none. | Startup confirms SER disabled; SER is not necessary for this reproduction. |

The varying last markers do not identify one faulty shader or prove that the queue reporting device loss caused it. `finishLighting` records a ray-tracing-write to compute-read barrier and schedules CPU history bookkeeping; it does not dispatch a shader. The original application fault remains unresolved.

The subsequent [no-dynamic control](../tmp/gpu-investigation/glass-no-dynamic.log) enabled RT at 13:22:07 with entity and particle rendering both disabled. The coordinating operator verified both overrides and observed 26,595 recorded frames and 1,862 ticks (approximately 93 seconds) without device loss.

### Particle isolation

- **Particles disabled, entities enabled:** the [final status](../tmp/gpu-investigation/glass-no-particles-status-final.json) verifies `particles.enabled=false` (overridden), `entities.enabled=true`, RT active, zero outstanding terrain builds, **33,805 recorded frames and 2,383 ticks** (approximately 119 seconds). No device loss was observed in this bounded [control](../tmp/gpu-investigation/glass-no-particles.log).
- **Particles enabled, entities disabled:** [glass-particles-only.log](../tmp/gpu-investigation/glass-particles-only.log) activated RT at 13:27:01 and reproduced the null GPU write at **13:28:33**, with latest recorded frame **27026**. The [JFR counter summary](../tmp/gpu-investigation/particles-counter-summary.json) contains 1,027 samples per counter: particles captured ranged **25–38** (mean 30.75), while ordinary and block entities captured and their geometry-submission counters stayed **zero**.

The particle-only fault reported graphics TOP and BOTTOM at the same START marker, `0x13531f`, for **frame 27025 NGX DLSS-RR evaluate**, command `0x231ce90f0d0`. Its same-command neighborhood contains the enclosing reconstruction START (`0x13531c`), feature-ensure START/END (`0x13531d`/`0x13531e`), and subsequently recorded evaluate/stage END markers (`0x135321`/`0x135322`). The END markers were recorded by the CPU but were not the driver-reported completion markers. Compute checkpoints were absent; `vkQueueSubmit2(compute)` first reported device loss. Fault data included `WRITE_INVALID 0x0` plus 74 `IP_UNKNOWN` records, with heap 0 usage 1661.84 MiB.

These controls prioritize particle geometry, its acceleration-structure updates, and reachable-resource lifetimes for audit. They do not prove a particular lifetime violation or that disabling particles fixes the application. The RR marker alone does not implicate NGX: the RAW control already reproduced the same null write without RR. Neither the causal relationship to the validation-layer retirement crash nor the original application fault is resolved.

### Serial graphics control — bounded survival

The default-off `-Dcaustica.debug.serial-frames=true` diagnostic changes Minecraft's existing submission wait to the just-submitted graphics timeline value. Actual host submission occurs before this wait; the existing five-second timeout and subsequent pool reset/retirement sequence remain unchanged. It removes overlap between host graphics submissions without serializing independent compute jobs. Client compilation and resource processing passed.

The [serial control with particles](../tmp/gpu-investigation/glass-particles-serial.log) started at 13:39:03, but returned to the original problem camera only at **13:39:44**. It remained alive until graceful shutdown at **13:42:58**, giving **3m14s at the reproducing camera**. The [final status](../tmp/gpu-investigation/glass-particles-serial-status-final.json) reports **46,423 recorded frames and 4,638 ticks**, RT active and unpaused, at position `(142.115, -49, -104.827)` with yaw 154.94952 and pitch 11.7. These cumulative counters include time before returning to that camera. Both entity and particle settings were enabled in the final status; the coordinating operator's recent counter verification showed **25–30 captured particles and zero ordinary/block entities**. [Recording metadata](../tmp/gpu-investigation/particles-serial-recording.json) identifies the JFR capture.

No device loss was observed in this bounded serial run. Its result supports investigating overlap-sensitive behavior but does not establish crash freedom, a root cause, or an acceptable performance fix. A separate 10,000-frame wait script was still pending at shutdown and is not evidence for the duration or outcome; the final status and log timestamps define this result.

## Bounded validation results

### Targeted synchronization and Aftermath runs

The later particle-only synchronization-validation run reached **99,609 recorded frames and 6,783 ticks** without device loss or a synchronization/VUID report. The requested settings enabled synchronization validation and startup confirmed the validation layer; this log did not print an information-level `CURRENT-VALIDATION-ENABLED` banner. See [log](../tmp/gpu-investigation/glass-particles-sync.log) and [recording metadata](../tmp/gpu-investigation/particles-sync-recording.json). This bounded survival does not establish that synchronization is correct.

After the user configured Aftermath, desktop inspection verified **Global** mode, shader debug information, resource tracking, and additional shader error reporting enabled, with call-stack capture disabled. The monitor was accessible without Windows elevation. [Recorded settings](../tmp/gpu-investigation/aftermath-applied-settings.json) identify the local dump and shader-information directories.

The parent's latest direct window inspection reconfirmed **Global** mode; **Generate Shader Debug Information: Yes**, **Resource Tracking: Yes**, **Call Stack Capturing: No**, and **Additional Shader Error Reporting: Yes**. [Current configuration screenshot](../tmp/gpu-investigation/aftermath-final-settings.jpg). No settings were changed and no UAC was requested. The UI still displays an administrator requirement for modifications; this observation does not establish edit access without elevation.

The subsequent [Aftermath particle run](../tmp/gpu-investigation/glass-aftermath.log) used the immutable cache-on renderer, without validation or serial-frame overrides. It returned to the original problem camera after frame 15,787, then exercised natural particles and 30 bursts of 200 flame particles. The [final status](../tmp/gpu-investigation/glass-aftermath-status-final.json) reports **65,841 recorded frames and 4,573 ticks**, including startup and camera movement. It exited gracefully without a GPU fault. [Stress samples](../tmp/gpu-investigation/glass-aftermath-stress.json) and [recording metadata](../tmp/gpu-investigation/glass-aftermath-recording.json) preserve the workload. No dump or shader-information files appeared in the configured directories. These observations do not establish a crash fix or explain the absence of shader-information files.

| Configuration | Observed result | Local evidence |
| --- | --- | --- |
| Core validation | Client exited successfully; Gradle elapsed 2m43s. Saved status: RT active, 2,045 recorded frames, zero outstanding terrain builds. | [log](../tmp/gpu-investigation/core-validation.log), [status](../tmp/gpu-investigation/core-status-now.json) |
| Synchronization validation | Client exited successfully; Gradle elapsed 1m50s. Saved status: RT active, 2,775 recorded frames, zero outstanding terrain builds. | [log](../tmp/gpu-investigation/sync-validation.log), [status](../tmp/gpu-investigation/sync-status.json), [settings](../tmp/gpu-investigation/sync/vk_layer_settings.txt) |
| GPU-AV master on; RT consistency on | Native access violation after RT startup; Gradle elapsed 34s. | [launch](../tmp/gpu-investigation/gpuav-rt-on-launch.log), [terminal result](../tmp/gpu-investigation/gpuav-rt-on-launch.stderr.log), [native stack](../tmp/gpu-investigation/gpuav-rt-on-native.log), [dump](../tmp/gpu-investigation/gpuav-rt-on-native.dmp) |
| GPU-AV master on; RT consistency off | Same native access violation; Gradle elapsed 34s. | [launch](../tmp/gpu-investigation/gpuav-rt-off-launch.log), [terminal result](../tmp/gpu-investigation/gpuav-rt-off-launch.stderr.log), [native stack](../tmp/gpu-investigation/gpuav-rt-off-native.log), [dump](../tmp/gpu-investigation/gpuav-rt-off-native.dmp) |

These are short, bounded observations, not crash-freedom or performance results. Gradle elapsed time includes startup and shutdown. Debug frame counters measure recorded RT composites, not GPU completion.

The GPU-AV pair used the same launch procedure and settings except RT consistency, with shader instrumentation and other buffer checks disabled. The master remained enabled in both. Both logs explicitly list `GPU-AV` under `CURRENT-VALIDATION-ENABLED`. The RT flag was set in both the settings file and the process-local `VK_KHRONOS_VALIDATION_GPUAV_RAY_TRACING_BUFFERS_CONSISTENCY` environment variable. See [launcher](../tmp/gpu-investigation/launch-watched.ps1), [on settings](../tmp/gpu-investigation/gpuav-rt/vk_layer_settings.txt), and [off settings](../tmp/gpu-investigation/gpuav-rt-off/vk_layer_settings.txt).

## Native GPU-AV failure

Both watched clients exited with `0xC0000005`, reading CPU address `0x500` in `VkLayer_khronos_validation.dll!gpuav::CommandBufferSubState::OnCompletion+0x4b`. The stack continues through `gpuav::QueueSubState::Retire`, `vvl::Queue::Retire`, and `vvl::Queue::ThreadFunc`.

Offline inspection of the off-control dump found `RCX=0` at DLL RVA `0x6881eb`. The instruction bytes `48 83 b9 00 05 00 00 00` decode to `cmp qword ptr [rcx+0x500],0`: a null command-buffer substate (`this`) at the initial postprocessing check, rather than a GPU validation report or an invalid mapped-buffer payload. The [available source](../tmp/gpuav_state_trackers.cpp) retrieves the GPU-AV substate during queue retirement before calling `OnCompletion`. A missing substate during retirement is the working hypothesis; its lifecycle cause is unproven.

The available layer source gives a concrete candidate lifecycle path:

- [CommandBuffer::Destroy](../tmp/vvl_cmd_buffer_state.cpp), lines 412–423, holds `WriteLock` only around `ResetCBState`, then destroys and clears `sub_states_` **outside that lock**.
- [QueueSubState::Retire](../tmp/gpuav_state_trackers.cpp), lines 644–653, takes the retained command buffer's `WriteLock`, obtains `SubState`, and calls `OnCompletion` unconditionally. The [SubState helper](../tmp/gpuav_state_trackers.h), lines 199–200, dereferences the returned pointer without checking it.
- `OnCompletion` explicitly anticipates concurrent command-buffer destruction, but its `NeedsPostProcess` check occurs inside the method, after dereferencing `this`. That check cannot handle a missing substate.

This exposes a post-destruction null-substate path and a lock-scope gap consistent with the native dump. **The available source has not been proven to match the installed validation DLL**, and the dump does not establish which destruction call triggered the sequence. The application audit found engine graphics pools retained through their graphics timeline, compute pools destroyed after completion waits, and host pool resets ordered after their prior submissions complete. Those observations establish GPU-completion ordering, not return from the corresponding graphics host submit: original graphics pool destruction can potentially overlap that call, as described above. No such overlap was captured at the fault. Asynchronous layer retirement and the potential application host-lifetime race remain distinct hypotheses; this layer-source finding does not explain `WRITE_INVALID 0x0` when validation is absent.

Earlier [file-only false control](../tmp/gpuav-explicit-control.log) still reported GPU-AV enabled, so it is not evidence that disabling the master failed. The earlier [explicit master-off run](../tmp/gpuav-off-lava.log), dated September 5, sets both `VK_KHRONOS_VALIDATION_GPUAV_ENABLE=false` and `VK_LAYER_GPUAV_ENABLE=false` and exits successfully after 6m10s. Together with the current core-validation success, this implicates the shared GPU-AV retirement path, not specifically RT consistency. The historical master-off run is not a same-build, same-workload paired control.

## Command-pool reuse GPU-AV control

The engine-only [pool-reuse candidate](../tmp/gpu-investigation/pool-reuse-candidate/README.md) retains native pools and resets them only after the existing GPU-completion condition. Graphics allocates one primary command buffer per outstanding lease; compute grows a retained pool to its batch high-water mark. This preserves queue ordering and resource-completion callbacks. All 67 engine tests passed, including ten pool-lifetime behavior tests.

With GPU-AV enabled and shader instrumentation/other buffer checks disabled, both bounded cave controls survived and exited with code zero:

| RT buffer consistency | Final recorded frames | Final ticks | Evidence |
| --- | ---: | ---: | --- |
| Enabled | 17,339 | 2,592 | [status](../tmp/gpu-investigation/pool-gpuav-on-status-final.json), [native watcher](../tmp/gpu-investigation/pool-gpuav-on-native.log) |
| Disabled | 14,179 | 2,178 | [status](../tmp/gpu-investigation/pool-gpuav-off-status-final.json), [native watcher](../tmp/gpu-investigation/pool-gpuav-off-native.log), [client log](../tmp/gpu-investigation/pool-gpuav-off.log) |

Both logs identify GPU-AV as enabled. No VUID was observed. Counters include startup; the explicit observation intervals each waited 12,000 additional recorded frames. The disabled control's shutdown summary reports **12 graphics pools created, 83,804 resets, 12 destroyed**, and **one compute pool created, 16,791 resets, one destroyed, 128 buffers**. Thus pool creations remain bounded while reuse grows substantially.

The earlier native `OnCompletion+0x4b` failure did not reproduce. This supports avoiding native command-pool destruction churn as a useful validation-layer control; it does not establish which destruction caused the previous race, prove crash freedom, or explain the separate no-validation null GPU write. The enabled control inherited a frozen logging-path issue that suppressed ordinary INFO messages; the disabled control uses an independent launch configuration with a proper file URI. Original frozen artifacts remain unchanged. No Windows UAC elevation was used.

### Current inline-query renderer with diagnostics: RT-only GPU-AV control

The current immutable inline-query renderer, pooled engine and host-result diagnostics completed the cave control in [20260912-170623-378](../tmp/shadow-query-gpuav/runs/20260912-170623-378). The [final status](../tmp/shadow-query-gpuav/runs/20260912-170623-378/status-final.json) records **7,441 frames / 3,267 client ticks**, RT active and unpaused, zero outstanding terrain builds, 3840×2054 output and four bounces. These are cumulative counters including startup, not the duration of the requested 2,400-client-tick observation interval; [progress samples](../tmp/shadow-query-gpuav/runs/20260912-170623-378/progress.json) preserve its intermediate observations. The [native watcher](../tmp/shadow-query-gpuav/runs/20260912-170623-378/native.log) records `PROCESS EXIT code=0x00000000`, and the [client log](../tmp/shadow-query-gpuav/runs/20260912-170623-378/cave.log) shows graceful shutdown. No VUID, device-loss report or native crash dump was found in this run; stderr is empty.

The startup `CURRENT-VALIDATION-ENABLED` banner explicitly lists **GPU-AV**, alongside core, parameter, object-lifetime and thread-safety checks. The [saved process environment](../tmp/shadow-query-gpuav/runs/20260912-170623-378/environment.json) selects SDK **1.4.357.0** through process-local `VK_LAYER_PATH` and confirms `gpuav_enable=true`, `gpuav_shader_instrumentation=false`, `gpuav_buffers_validation=true` and `gpuav_ray_tracing_buffers_consistency=true`. The [settings file](../tmp/shadow-query-gpuav/vk_layer_settings.txt) matches the prior RT-consistency-on pool control; the other GPU-AV buffer checks, including general acceleration-structure-build validation, are disabled. The independent [launcher](../tmp/shadow-query-gpuav/launch.ps1) uses a proper Log4j file URI and the native crash watcher, without changing Aftermath settings or requesting UAC elevation.

Here, **RT-only describes the selected GPU-AV buffer checks**, not the ordinary validation checks. RT buffer consistency checks BLAS build/update index-buffer equality and active/inactive primitive status. Shader instrumentation is disabled, so this result does not directly validate `OpRayQueryInitializeKHR` or shader memory accesses. The shutdown summaries report **12 graphics pools created / 42,900 resets / 12 destroyed**, and **one compute pool created / 16,806 resets / one destroyed / 128-buffer high-water mark**. This is bounded survival of the current query-plus-diagnostics candidate; neither the earlier GPU-AV native failure nor the no-validation DMA fault has an established root cause or demonstrated fix.

### Paired particle control without validation

The same frozen blocker renderer and host instrumentation were launched with the pooled and original engine variants. Both used normal graphics overlap, Aftermath Global mode, checkpoints, exact shader exports, the original problem camera, zero ordinary/block entities, 40 bursts of 200 flame particles two seconds apart, then 100 seconds of natural particles.

Both completed without a GPU fault: the pooled run ended at **66,909 recorded frames / 4,319 ticks**, the original-pool run at **57,874 frames / 4,226 ticks**. Recording counters verify ordinary/block entities remained zero; captured particles averaged 38.03 (range 10–78) and 38.42 (10–84), respectively. [Pooled status/recording](../tmp/gpu-investigation/glass-pool-crash-control.json), [original status/recording](../tmp/gpu-investigation/glass-original-pools-crash-control.json), [pooled counters](../tmp/gpu-investigation/glass-pool-crash-counters.json), [original counters](../tmp/gpu-investigation/glass-original-pools-crash-counters.json).

The pooled shutdown reports 16 graphics pools across 317,152 resets, and one compute pool across 168,972 resets with 27 command buffers. These observations confirm bounded pool reuse under dynamic particle geometry. Because the original-pool control also survived, this pair **does not establish a fix for the application GPU fault**. No new Aftermath dump appeared.

## Micromap scratch alignment correction

[RtOpacityMicromap.java](../packages/renderer-raytracing/src/main/java/dev/comfyfluffy/caustica/renderer/raytracing/accel/RtOpacityMicromap.java) now allocates scratch using `context.accelerationStructureScratchAlignment()`; data and triangle-array addresses retain their fixed 256-byte alignment. [Vulkan requires scratch alignment to follow the device property](https://docs.vulkan.org/refpages/latest/refpages/source/vkCmdBuildMicromapsEXT.html), rather than a hardcoded constant.

The recorded GPU reports scratch alignment 128, so the previous 256 alignment already satisfied this device. This contract correction is not an explanation or demonstrated fix for either crash.

Validation: `gradlew.bat :packages:renderer-raytracing:test --tests dev.comfyfluffy.caustica.renderer.raytracing.accel.RtOpacityMicromapTest --console=plain` passed: two tests, zero failures/errors. The tests check native usage descriptions and triangle descriptor packing; they do not reproduce GPU execution. [JUnit result](../packages/renderer-raytracing/build/test-results/test/TEST-dev.comfyfluffy.caustica.renderer.raytracing.accel.RtOpacityMicromapTest.xml).

## Queued branches + MAX compiler: RT-buffer-consistency soak

The frozen queued-branch renderer, MAX Slang runtime, pooled engine and existing CPU/crash diagnostics completed [run 20260912-185609-866](../tmp/queue-maximal-gpuav/MEASUREMENT.md). The parent used `observe.py` for **four 600-client-tick waits**, not the prepared `soak.py`. Initial/final snapshots advanced **1,295/744 → 6,705/3,149 frames/ticks**, an observed **5,410 frames / 2,405 ticks**; final totals include startup. Both endpoints show RT active and unpaused, unchanged camera/settings, **3840×2054**, ray reconstruction quality 0, four bounces, FG/Reflex off. Outstanding terrain builds fell from 34 initially to zero at each of the four progress samples.

The [offline audit](../tmp/queue-maximal-gpuav/runs/20260912-185609-866/audit.json) finds all **17 process VK environment entries exactly match** the expected matrix, without extra entries. SDK 1.4.357.0 is selected; GPUAV enable, buffers validation and RT-buffer consistency are true, shader instrumentation and the other explicitly selected GPUAV buffer checks are false. Startup explicitly confirms GPU-AV alongside core, parameter, object-lifetime, thread-safety checks and handle wrapping, and confirms the frozen MAX runtime path (Slang 2026.14.1), SER, opacity micromaps and device-fault diagnostics enabled. **RT-only describes the GPUAV buffer checks, not disabling ordinary validation; shader instrumentation did not run.**

The [client log](../tmp/queue-maximal-gpuav/runs/20260912-185609-866/cave.log) contains zero VUIDs, zero Validation Error messages and no device-loss report. The [native watcher](../tmp/queue-maximal-gpuav/runs/20260912-185609-866/native.log) records `PROCESS EXIT code=0x00000000`, zero second-chance exceptions; stderr is empty and the run directory contains no native/GPU dump. The watcher did record 64 nonfatal first-chance exceptions (breakpoint ×1, illegal instruction ×30, thread-name ×1, C++ ×32). Two validation warnings report combined core/GPUAV checks and forced device-feature settings; unrelated startup errors include Windows counters, missing Nsight manifests, HTTP authentication, resource paths and NGX load probes. These are preserved rather than describing the entire log as error-free.

Shutdown reports **9 graphics pools created / 27,015 resets / 9 destroyed**, pool/buffer high-water marks 9/9; **1 compute pool created / 18,200 resets / 1 destroyed**, pool/buffer high-water marks 1/128. This establishes bounded completion with substantial pool reuse for this configuration. It does not establish a fix for the earlier validation-layer native crash or no-validation GPU fault, validate shader memory/ray-query operands, or support a performance claim. The offline audit made no launcher/runtime changes and launched no game.

## Host retirement ownership audit

The [read-only host retirement audit](../tmp/gpu-investigation/host-retirement-audit.md) checked current ownership paths and frozen original-engine/Minecraft 26.2 bytecode. It confirms a code-permitted sequence in which graphics timeline completion releases the original command-buffer pool before the native host submit returns. Current pool reuse avoids that destruction/reset window: retirement returns a Java lease, and native reset occurs on a later render-thread acquisition. Normal compute submission, waiting and completion callbacks run sequentially, so compute retirement follows submit return.

No additional buffer, descriptor or acceleration-structure lifetime violation was established from early graphics retirement alone. The inspected frame/current-and-previous-scene claims retain the relevant resources through their GPU consumers; direct host-call object lifetime and external queue synchronization do not establish a blanket submit-return requirement for every recorded resource. The audit found no evidence tying this possible pool overlap to the original `WRITE_INVALID 0x0` fault. Existing pool/RT-buffer-consistency survival remains bounded evidence, with shader instrumentation disabled. No reproduction, launch or runtime change was performed.

## Composition implementation-resource ownership correction

The separate [publication audit](../tmp/gpu-investigation/descriptor-publication-audit.md) identified and reproduced a program-resource ownership gap: compositions A and B can both contain registration R, then C removes R. If B retires before an older retained A, the previous implementation released R's resource owner even though A's implementation table still contained its address. This does not require the command-pool host-submit window. No same-frame stale or zero TLAS, geometry-table or light-table publication was established in the audited normal scene path.

[ProgramSession](../packages/engine/src/main/java/dev/comfyfluffy/caustica/engine/program/ProgramSession.java) now gives each compiler candidate and published composition independent shared claims on its registrations and their implementation resources. Failed/discarded candidates release their claims; a published composition releases only its own claims when its backend retirement callback arrives. Registration teardown and channel drainage wait for the final composition claim. A later composition's exclusive resources can retire while an older composition remains live. The change adds no predecessor chain, GPU wait, shader modification or renderer-backend API change.

The A/B/C behavioral regression failed against the saved pre-fix source (`expected 0 releases, actual 1` while A remained retained) and passes after the correction, including B-exclusive release and channel-drain assertions. Existing failed/cancelled compilation tests and a new synchronous compiler-exception test pass. Engine, raytracing and renderer-runtime checks passed **93 + 213 + 73 = 379 tests**, zero failures/errors/skips, alongside ABI, generated-layout, SPIR-V-target, package and ownership checks. [Evidence and staged overlay](../tmp/gpu-investigation/composition-ownership-fix/README.md) preserve the source hashes, test results and unchanged public API check.

This is a demonstrated ownership defect and tested correction, **not an established explanation or fix for the original GPU fault**. The inspected 14:36 crash log did not show the required repeated composition-publication sequence. The existing `final-retained-20260912` classpath and artifacts remain unchanged.

Separate [combined soak inputs](../tmp/composition-ownership-soak-20260912/README.md) prepend the verified ProgramSession overlay to the original 173 frozen dependencies. Cave/glass argfiles are prepared for the existing BDA/ray and sync/RT profiles, plus ordinary reload testing, at requested 1280×720 launch dimensions. They do not replace the retained performance baseline.

### Sync/RT soak and registration lifecycle coverage

The parent-controlled combined runtime completed the cave soak and **three off/on cycles (six transitions) in 46.39 seconds**, then exited normally as PID39408. The [bounded run audit](../tmp/composition-ownership-soak-20260912/runs/sync-rt-cave-20260912-200142-593/AUDIT.md) and [machine-readable evidence](../tmp/composition-ownership-soak-20260912/runs/sync-rt-cave-20260912-200142-593/audit.json) preserve all six settled snapshots, transition log times, environment comparisons and file hashes. Initial/final snapshots advance **1,938/533 → 40,202/5,337 frames/ticks**, an observed **38,264 frames / 4,804 client ticks** spanning soak and cycles. Endpoints have RT active/unpaused, actual **1280×720** and unchanged camera/settings; final terrain builds are zero. The initial snapshot precedes the tick freeze: world game time is **4,473,714 initially**, then **4,474,170** in the saved soak, cycle and final snapshots. World simulation was frozen during the bounded wait and cycles, not across the whole initial/final interval. The completed wait job separately ends at 27,608 frames / 3,393 ticks. These cumulative observations do not define the exact requested 2,400-tick wait interval or directly count completed GPU work.

All **51 expected VK environment entries match**; the sole extra saved entry is empty `VK_SDK_PATH`. Argfile and settings hashes match the prepared sync profile. Loader output confirms SDK **1.4.357.0**, and startup lists synchronization and GPU-AV alongside core, parameter, object-lifetime, thread-safety and handle checks. The selected matrix enables submit-time synchronization and RT-buffer consistency, with **shader instrumentation, BDA and trace-ray checks disabled**. It does not directly validate shader memory or ray-query operands.

The complete client log and stderr contain **zero VUIDs, Validation Error messages, SYNC-HAZARD reports, device-loss codes or WRITE_INVALID reports**. The native watcher records **exit 0, zero second-chance exceptions**, and 85 first-chance exceptions (breakpoint ×1, illegal instruction ×27, thread-name ×1, C++ ×56). No native/GPU dump is present in this run, nor a PID39408-named Aftermath dump in the shared directory. Stderr contains loader output; two validation warnings concern combined checks and forced device features. Unrelated startup/resource/NGX errors remain, so the logs are not described as error-free. Shutdown reports **12 graphics pools created / 161,732 resets / 12 destroyed**, high-water marks 12 pools/12 buffers; **1 compute pool created / 40,366 resets / 1 destroyed**, high-water marks 1/128.

Each off state persisted across matching samples at least one second apart; returns to active advanced **129, 136 and 151 recorded frames** beyond their first matching samples, exceeding the helper's 30-frame threshold. The [lifecycle source audit](../tmp/gpu-investigation/composition-ownership-fix/registration-lifecycle-plan.md) establishes that these transitions close and recreate program registrations across new sessions. Runtime status does not expose registration IDs, final owner releases or composition retirement events. This exercises registration lifecycle and bounded survival, **not the controlled same-session A/B/C retirement ordering**; that remains covered by the behavioral regression. It supplies no performance claim or demonstrated fix for the original GPU fault. The offline audit made no game/API calls or production source changes.

## Instrumented null shader reads: separate from the original null-write faults

The combined BDA+ray instrumentation run, PID **59776**, failed at **2026-09-12 19:51:03** with graphics/GPC **READ of address 0**, rather than the earlier command-processing `WRITE_INVALID 0` signature. The [run audit](../tmp/retained-validation-next/runs/bda-ray-cave-20260912-195017-996/AUDIT.md), [decoded dump](../tmp/retained-validation-next/runs/bda-ray-cave-20260912-195017-996/aftermath-with-nvdbg.json), and copied crash report preserve evidence. Decoder reports `Error_DMA_PageFault`, engine reset true, adapter reset false, and a shader-instruction MMU fault at **PC 0x130**, hash **197285700DAACFDD** (decimal 1833674714605342685). Parent's Nsight GUI identifies **Ray Generation(main), ray_tracing_11**, multiple warps at the same fault PC, selected-warp current PC **0x170**, and CommandQueue1 markers not-started **0x33d66** / finished **0x33d65**. CLI uses the synthetic name `ray_tracing_01`; the common hash and fault PC identify the same fault despite display-name differences.

Instrumentation was active: positive BDA groups included 256 and 383, with positive ray groups 1 and 3. No VUID preceded device loss. The first explicit host observer was compute `vkWaitSemaphores`; this does not assign GPU causality to compute. The native watcher recorded process exit `0xffffffff` without a second-chance CPU exception. Actual output was 1280×720 with RR 640×360 input, but initial HTTP status was empty: no completed baseline/soak or performance claim follows. Module dumping was disabled, and adding available `.nvdbg` files left the shader mapping null. **The exact old binary is unavailable**, so neither the source instruction nor application-versus-instrumentation origin is established. Pinned layer source shows additional heap reporting/BDA table-pointer indirections, not proof that any such pointer was null.

The isolated **BDA-only** run, PID **52996**, subsequently failed at **20:07:55**, again with a graphics/GPC shader null read and no second-chance native exception. The [completed audit](../tmp/retained-validation-split/runs/bda-only-cave-20260912-200715-965/AUDIT.md) records exact environment agreement, positive BDA instrumentation, ray instrumentation disabled, no VUID, and 329 preserved module dumps (319 before / 10 after). This new fault has a different hash **5D20A2748104FA0F**, fault **PC 0x60**, and an exact SDK hash match to [dump_305_after.spv](../tmp/retained-validation-split/runs/bda-only-cave-20260912-200715-965/instrumented-modules/dump_305_after.spv), raygen `main`. With that binary and nvdbg search paths, the [decoder maps](../tmp/retained-validation-split/runs/bda-only-cave-20260912-200715-965/aftermath-with-modules.json) to synthetic **rgen.10000.spv:36573**; this is not yet an attributed Slang source line or proof of an injected-code defect. All archived binaries were hashed, and none matches the older combined-run hash.

Thus ray instrumentation is not necessary for this new null-read failure category. These controls use the old frozen registry, and neither establishes that the composition ownership gap caused the fault, that GPU-AV itself is defective, or that the original no-validation null-write fault shares a cause. The [offline hash helper](../tmp/retained-validation-split/hash_modules.py) accepts future archived modules and fault hashes; new hashes must be matched before claiming an exact source mapping. No game launch, global validation setting change or UAC request was made by this audit.

### BDA-only exact injected-code mapping and ray-only bounded control

Subsequent symbol reload **resolves the BDA-only fault attribution left open above**. Parent's GUI shows `ray_tracing_12`, hash **5D20A2748104FA0F**, red fault **PC 0x60** at SPIR-V row **36573**, `%37232 = OpLoad %ulong %37231 Aligned 8`, in **inst_buffer_device_address_range**. CLI with the matched binary/nvdbg independently reports `rgen.10000.spv:36573`; its synthetic shader name remains `ray_tracing_01`. The yellow current-PC marker at row 36588 is separate. The faulting load is **in injected BDA range validation**, not the application's checked load. This identifies the operation, not yet why its validation data is invalid.

The [exact-chain/source audit](../tmp/retained-validation-split/bda-injected-read.md) preserves the matched SPIR-V: set 31/binding 6 supplies a snapshot-pointer slot; `%37226` loads the physical snapshot pointer, `%37182` is a private cached **index initialized to zero**, `%37228` selects its range, and the fault loads that range's end. The effective SPIR-V expression is `tableBase + 16 + 16*indexCache`. Reported null-page address/0x1000 precision does not expose those register values. Pinned SDK source unconditionally reads the cached range before checking the count; it also provides explicit pre-submit table population, a pointer copy and barriers. Missing index initialization and a blanket missing-copy/barrier claim are unsupported. Snapshot/root corruption, missed setup, lifetime issues, zero count and driver translation remain unproven possibilities; the older combined fault still lacks its exact binary.

The [ray-only audit](../tmp/retained-validation-split/runs/ray-only-cave-20260912-200856-398/AUDIT.md) confirms positive ray instrumentation **1,3,1,3**, BDA disabled, expected environment agreement, no VUID/device loss, and normal process/watcher exit zero. Initial/final status is **6,126/1,089 → 47,727/5,000 frames/ticks**, a bounded delta **41,601 frames / 3,911 ticks**, RT active and unpaused at 1280×720 with no outstanding terrain builds at either endpoint. The wait response **36,094/3,962** is an intermediate snapshot, not the final total. Of 340 archived modules, 63 are unchanged, 266 changed and 11 new; twelve after-files include eight inherited BDA files and four changed/new ray-run files. Their hashes and provenance are preserved separately.

**Ray-only survival does not validate the shared internal address-table root:** valid ray operands can avoid error reporting and its shared table dereferences, whereas BDA always loads range data. The reserved push-data pointer, per-action mapping table and BDA snapshot chain therefore all remain relevant. Confucius independently owns the application push-data overlap audit. This evidence does not identify a root cause for the original no-validation null writes, validate an ownership fix, or justify a performance claim. No launch or runtime/source change was performed by these offline audits.

The subsequent [push-data reservation audit](../tmp/gpu-investigation/push-data-reservation-audit.md) excludes overlap from the inspected application RT update: its roots occupy **[0,144)**, while the layer reservation begins at **at least 248** under the reported Vulkan 1.4 device contract. This is a bounded exclusion of that direct overwrite mechanism, not proof of valid runtime layer state. The callback source audit connects command-buffer begin registration, trace action setup and queue pre-submit snapshot copy; it found no demonstrated skipped callback or failed copy. Actual root/table/index values remain unavailable. No further instrumentation run or layer build is proposed from these findings alone.
