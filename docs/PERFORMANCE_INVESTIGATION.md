# Cave performance investigation — updated 2026-09-13

Target: 60 rendered frames/s at 3840×2054, DLSS Ray Reconstruction Performance (1920×1027 internal), four bounces, in New World (2). The enclosed-cave camera is `-1632.0144550370237 87 -6503.699999988079`, yaw `45.90098`, pitch `15.000088`. Frame generation and Reflex are disabled. Aftermath Global mode remains enabled with shader information, resource tracking and additional shader error reporting.

**The retained build records 50.51 composites/s; the isolated scalar-SoA candidate records 56.651 and is not production-promoted. The 60 FPS GPU target remains unmet.** The retained combined recording includes queued stable-plane branches, the MAXIMAL compiler, pooled command buffers, composition ownership correction and direct instance-input construction. Mean composite-start interval is 19.797 ms, with P99 20.823 ms, across 1,804 bounded windows. These are renderer composite timestamps, not scanout or generated frames. Candidate GPU results do not replace the separately measured CPU bound below.

**The recorded CPU target passes at 3.072 ms P99** for the conservative host-inclusive mod-cost bound. Scene-preparation worker allocation fell from 159.98 to 65.98 MB/s in the measured pair. Entity populations differ, so the whole rate reduction and render-thread change are not attributed solely to the optimization. Normal build classes match the measured artifact, and engine/renderer/runtime checks pass. [Latest retained measurement](../tmp/instance-table-direct-input/runs/cave-20260912-214247-734/measurement-20260912-214359/MEASUREMENT.md). The faster 51.47 FPS single-query experiment remains unpromoted for the configuration and optical limits documented below.

## Measurements

### Scratch-layout candidate, not retained

The isolated scalar-stream layout records **56.651053 composites/s** versus fresh retained control **50.670972**, an observed 11.80% increase. Control JFR `1e90dcb8-d01b-4965-b818-21918a67bc79` and SoA `de8fd54c-4767-41e0-9430-60da321fd173` provide 904/908 consecutive bounded windows. Mean composite-start interval improves **19.735165 → 17.651923 ms**, saving 2.083242 ms; approximately **0.985256 ms** still separates that mean from 60 FPS. BUILD improves **3.874543 → 1.000344 ms**, while FILL regresses **10.234554 → 11.030287 ms** (+0.795734 ms). This is a strong net layout result with a material FILL tradeoff, not a 60 FPS result. [Paired cadence and workload](../tmp/scratch-soa/CADENCE_COMPARISON.json).

Selected-frame-window graphics clocks are **control 2914.706 → SoA 2919.063 MHz** (+0.15%), with memory clocks unchanged. This direction corrects the earlier reversed clock transcription. Both runs have zero outstanding terrain work at their boundaries; entities/particles are **96/4 → 95/5**. Boundary counters do not prove absence of all terrain work, and different populations/clocks prevent attributing every timing delta exclusively to layout. Nsight diagnostic intervals are not substituted for ordinary JFR cadence.

The candidate changes scalar-word addressing in the existing 64-byte logical path and 112-byte stable-record scratch buffers, including every BUILD/FILL/guide and NRD consumer. At actual 1920×1027 tracing, allocation remains **378,593,280 + 662,538,240 bytes**: no extra full record, transpose buffer, dispatch or sample/bounce reduction. All 11 actual RT entries and NRD compile; 48 raw/normalized SPIR-V validations, 16 ABI comparisons and compiled-accessor bit-pattern/address checks passed. NRD uses the original production CLI settings; its rebuilt unmodified control matches frozen normalized SPIR-V exactly. These offline gates do not establish runtime optical equivalence.

**Scalar SoA is still not production-promoted.** Matched RR/REBLUR/RELAX guide and encoding checks completed across 728 EXRs, with all recorded NRD stages present. Longer noisy RR clear-transmission sampling remains pending; two captures do not resolve the observed radiance difference. See the [matched optical review](../tmp/scratch-soa/optics/numeric-review/MATCHED_REVIEW.md) and [guide findings](STABLE_PLANE_GUIDES.md). Exact artifact: `tmp/scratch-soa/scratch-soa-overlay-v2.jar`, SHA-256 `97dfdd5c3d847ac55af1ed8f72b87aa00e3f0ba202c0ccc696fe45badf27192a`; manifest SHA-256 `ce4de54af9487e695d8dc88874b9ee06cd46d6608271f6c61cba3c4d7ed7b8bd`. [Manifest and inherited classpath](../tmp/scratch-soa/manifest.json), [cave args](../tmp/scratch-soa/cave.args), [glass args](../tmp/scratch-soa/glass-1280x720.args), [verification and limits](../tmp/scratch-soa/README.md). The separate [175-entry AoS optical control](../tmp/scratch-optical-control/manifest.json) adds no shader overlay; the [new pinned capture wrapper](../tmp/scratch-optical-control/capture-optical-v2.py) supports control and SoA without changing the prior wrapper. The [NRD acceptance recipe](../tmp/gpu-investigation/SCRATCH_SOA_NRD_ACCEPTANCE.md) distinguishes plane-0 inputs from dominant guides and requires matched method-specific checks.

The **AoSoA32 variant is rejected for promotion over scalar SoA**. Recording `66bf7b1e-8d03-459b-8a13-c2a7c513f284` records **56.753883 FPS / 17.619940 ms**, only 0.18% faster than SoA. BUILD changes **1.000344 → 0.951728 ms**, but FILL changes **11.030287 → 11.080507 ms**: the intended FILL recovery was not observed, and BUILD+FILL is slightly worse. RR drops **3.956600 → 3.856002 ms** despite unchanged RR implementation. SoA→AoSoA entities/particles are **95/5 → 85/15**, graphics clocks **2919.063 → 2907.438 MHz**. The tiny net difference cannot be assigned confidently to tiling and does not justify tile/ragged-tail addressing complexity. PID 49860 exited normally; no optical promotion is claimed. [Comparison](../tmp/scratch-aosoa32/CADENCE_COMPARISON.json), [decision and limitations](../tmp/scratch-aosoa32/TIMING_ANALYSIS.md). Frozen artifact `scratch-aosoa32-overlay.jar` SHA-256 `c32feef980689f8dbd46a140de705d82cf01c57112f8f49cc5b5c304b87643f0` and its [manifest](../tmp/scratch-aosoa32/manifest.json) remain preserved, unpromoted.

Each comparison uses a frozen classpath, a stationary camera, frozen world ticks, zero outstanding terrain builds at the sampled boundaries, warmup and roughly 900 recorded frames. Some recordings still contain host terrain snapshot/copy work; boundary counters alone do not establish settled terrain. Entity populations can change across launches; these are bounded observations, not perfectly identical scene snapshots. No compilation or profiling export runs during the timing interval.

| Variant | Frame envelope mean ms | Build planes ms | Fill planes ms | Trace/resources ms | RR ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| Current-hit/history separation, Aftermath on | 28.41 | 5.37 | 17.56 | 23.70 | 3.97 |
| Dedicated 36-byte shadow payload | 27.68 | 5.25 | 16.88 | 22.93 | 3.97 |
| Certified opaque shadow blockers | 26.89 | 5.30 | 16.09 | 22.15 | 3.99 |
| Compact 28-byte radiance intersection | 22.06 | 4.67 | 11.89 | 17.33 | 3.96 |
| Direct SER hit-object extraction | 21.62 | 4.58 | 11.37 | 16.80 | 3.98 |
| Inline shadow ray queries, pooled engine | 20.65 | 4.62 | 10.50 | 15.92 | 3.96 |
| Future branches queued in existing records | 19.97 | 3.94 | 10.50 | 15.24 | 3.98 |
| Queued branches, Slang MAXIMAL | 19.79 | 3.89 | 10.38 | 15.05 | 4.00 |

The compact-payload change moves material evaluation to ray generation. Closest-hit returns only the geometry/primitive identity, barycentrics and distance, plus path state. The resolver reads the retained current geometry and evaluates surface/volume properties; only guide building requests previous geometry. This reduces the live ray payload while retaining the material API and transport settings.

Evidence: [shadow recording](../tmp/gpu-investigation/cave-shadow-payload.json), [blocker recording](../tmp/gpu-investigation/cave-blocker.json), [compact recording](../tmp/compact-payload/cave.json), [compact stage summary](../tmp/compact-payload/cave-summary.json), [cadence and optical comparison](../tmp/compact-payload/comparison.json), [immutable compact artifact manifest](../tmp/compact-payload/manifest.json).

The initial Nsight GPU Trace contains five captured frames: [trace](../tmp/gpu-investigation/cave-baseline.ngfx-gputrace), [export](../tmp/gpu-investigation/cave-nsight-analysis.yaml). Stable-plane fill occupied about 53–58% of those captured frames; shader memory/material work was a significant contributor. Capture overhead and changed entity populations prevent treating those frames as the timing baseline for later JFR runs.

Direct SER extraction reads accepted triangle identity, barycentrics and hit distance from the reordered hit object without invoking a closest-hit shader. Slang's `GetRayDesc().TMax` emits the hit-object ray-limit operation; the separate miss branch preserves the miss sentinel. Eleven native stages pass SPIR-V validation. [Immutable candidate](../tmp/ser-direct-compact/manifest.json), [stage results](../tmp/ser-direct-compact/cave-summary.json), [cadence](../tmp/ser-direct-compact/cadence.json).

The fresh five-frame [Nsight trace](../tmp/ser-direct-compact/cave.ngfx-gputrace) and [analysis](../tmp/ser-direct-compact/nsight-analysis.yaml) were captured through Nsight's UI after warmup at the specified cave camera. Fill remains 52.9–57.6% of captured frame duration. Its first captured dispatch averages 13.82 active warps; dependent local/global reads average about 521 cycles and texture/surface reads about 920 cycles. L1TEX and L2 hit rates are 61.0% and 80.3%. These trace metrics point to memory/shader latency for further investigation; Nsight's estimated frame gains are not measured savings. This client used original engine pools and survived through capture and graceful shutdown.

Nsight's Hotspots view further locates the highest individual source shares in `retained_lights.slang`: light-record/type access at lines 154 and 145 (2.10% and 2.07% of whole-trace samples), global proposal PDF at line 56 (1.63%), and local CDF search at line 132 (1.40%). Their leading stalls are long-scoreboard dependencies (98%, 100%, 93% and 84%). These locations prioritize light sampling's dependent loads; they are not a claim that all stalls originate in material textures.

Inline shadow queries remove shadow shader dispatch and payload transfer from fill. Candidates with certified zero transmission terminate visibility immediately; other candidates use the same coverage test and continue traversal to the nearest accepted surface. Surface transmission and ordered volume entry/exit then run in ray generation. The paired combined baseline records fill 11.366 ms and trace/resources 16.732 ms, versus 10.503 and 15.920 ms for the query candidate. [Stage results](../tmp/shadow-query-prototype/cave-summary.json), [cadence](../tmp/shadow-query-prototype/cadence.json), [semantic correspondence and artifact](../tmp/shadow-query-prototype/README.md). All eleven native stages and full renderer checks pass; the promoted diagnostic artifact preserves the new native-result and query checks.

Queued future branches reuse the existing endpoint and guide allocations instead of a three-element shader-local branch array. The current branch stays local; enqueue/dequeue preserves the full path, transform and motion without compression. This follows RTXPT's exploration-storage pattern and adds no allocation or dispatch. Matched build mean improves from 4.594 to 3.940 ms while fill remains unchanged. [Cadence and workload comparison](../tmp/branch-record-queue/cadence-comparison.json), [layout, ownership and native verification](../tmp/branch-record-queue/README.md).

The Slang session's explicit MAXIMAL option reduces composite-start mean by 1.30% against a rebuilt default-option control on the same queued renderer. Fill improves from 10.571 to 10.385 ms; RR stays near 4.000 ms. The candidate's sampled graphics clock is 0.55% lower, though entity/particle populations differ, so this is a bounded pair rather than an isolated universal compiler speedup. Both runtime shims preserve standard shader debug information and the original floating-point mode. Sixty-six native modules passed packaged compilation, external validation, and reflected layout/interface comparison. The candidate's initial pipeline stack is 320 bytes for build and 192 for fill, matching the queue's earlier initial composition. [Cadence, populations and clocks](../tmp/radiance-pipeline-prototype/slang-maximal/CADENCE_COMPARISON.md), [compiler provenance and verification](../tmp/radiance-pipeline-prototype/slang-maximal/README.md).

The option is retained in production source. The production native CMake output and normal runtime resources were refreshed because Gradle alone does not rebuild the C++ shim. The runtime check passed all 16 tests, and native executable code matches the approved prototype. The normal bundle is `c2a498e926dbd4cdb56ee5db2bc28d8d6baf563b83f17903237c2615dbedbbcd`. [Normal development refresh](../tmp/radiance-pipeline-prototype/slang-maximal/normal-dev-refresh/verification.json). A separate [immutable combined classpath](../tmp/final-retained-20260912/README.md) preserves the tested queue, compiler and crash diagnostics for follow-up measurements.

## Rejected experiment

A full endpoint surface-closure cache added 112 bytes per plane, about 632 MiB of scratch at the target internal resolution. It saved fill time but increased build and memory cost: matched clean frame envelopes were 30.049 ms with the cache and 29.847 ms without it. That implementation was removed. Initial contaminated measurements taken while a stale game process remained alive are excluded.

A later 16-byte intersection-only endpoint cache also regressed: mean frame 24.89 ms, build 5.21 ms, fill 14.03 ms. An immediate repeat of the immutable compact baseline returned to 22.06 ms, build 4.63 ms and fill 11.90 ms. [Endpoint measurement](../tmp/endpoint-cache/cave-summary.json), [baseline repeat](../tmp/compact-payload/cave-repeat-summary.json). The repeat's [GPU telemetry](../tmp/compact-payload/cave-repeat.gpu.csv) records approximately 2.92 GHz graphics clocks, 16 GHz memory clocks and 70°C during load. The endpoint candidate is rejected; removing a trace did not offset its additional state/storage costs. The precise compiler/register/memory contribution is not established.

Removing unused shadow shader stages from the query pipeline did not reduce the reported stack: both variants require 608 bytes for build and 192 for fill. The reduced pipeline records 20.535 ms versus a matched full-pipeline baseline of 20.630 ms, only 0.46%; this does not establish a useful performance gain. It remains unpromoted. [Candidate summary](../tmp/radiance-pipeline-prototype/cave-summary.json), [matched baseline](../tmp/radiance-pipeline-prototype/cave-baseline-summary.json).

Preserving the outer stable-plane loop instead of unrolling it regressed build from 4.594 to 5.921 ms, with fill unchanged at 10.516 ms. The frame envelope rose to 21.948 ms. This shader-only candidate is rejected without further optical testing. [Measurement](../tmp/radiance-pipeline-prototype/build-loop/cave-summary.json), [candidate details](../tmp/radiance-pipeline-prototype/build-loop/README.md).

An exact 64-entry local proposal proxy table reduced fill to 10.297 ms from 10.506 ms, while local bake rose to 0.154 ms. Its frame envelope of 20.585 ms is only slightly below the matched 20.630 ms baseline. The table adds 15.1 MiB per retained frame at the target internal resolution. This remains experimental; the result does not yet justify promoting the memory tradeoff. [Measurement](../tmp/local-proxy-prototype/cave-summary.json), [layout and distribution checks](../tmp/local-proxy-prototype/README.md).

Caching each global proposal's integer mass beside its light index also regressed: fill increased from 10.506 to 10.796 ms and the frame envelope from 20.630 to 21.037 ms. This isolated candidate preserved the original local distribution and doubled global proxy storage. It is rejected; removing one dependent fetch did not yield a measured benefit. [Measurement](../tmp/global-proxy-prototype/cave-summary.json), [distribution and synchronization checks](../tmp/global-proxy-prototype/README.md).

Extending direct HitObject extraction with reordering to the initially coherent traces regressed both isolated variants. Initial-fill extraction reduced its reported group stack from 192 to 64 bytes, but fill rose to 10.971 ms and frame time to 21.089 ms. Build-only extraction reported 128 bytes for build, but build rose to 6.852 ms and frame time to 22.922 ms. Both variants are rejected. The reported stack is not a performance result; baseline logs also contain composition changes, so stack values must be matched by composition. [Initial fill](../tmp/direct-initial-hitobject/initial-fill/cave-summary.json), [build only](../tmp/direct-initial-hitobject/build-only/cave-summary.json), [semantics and routing](../tmp/direct-initial-hitobject/README.md).

The fresh retained-query [Nsight capture](../tmp/shadow-query-diagnostics/cave.ngfx-gputrace), collected at 17:50:10, contains five frames totaling 125.16 ms. Its [analysis export](../tmp/shadow-query-diagnostics/nsight-analysis.yaml) preserves per-frame metrics. The Hotspots UI shows `trace_transport.slang:171` (`RayQuery.Proceed`) at 6.25% of whole-trace samples, with a 79% long-scoreboard leading stall. Light-record access remains prominent at `retained_lights.slang:154` (1.95%, 98% leading long-scoreboard stall) and line 145 (about 1.88%, 100%). Global proposal PDF at line 56 is about 1.54% (91%). These are source-attributed sampling shares, not isolated instruction timings or achievable gains.

Reusing guide-output fields for an exact endpoint intersection cache added no capacity, but did not improve fill: 10.502 ms versus 10.506 ms baseline. Build increased to 4.678 ms and the frame envelope to 20.724 ms. This cache is rejected. Its first package failed because the new codec module was absent from the runtime's explicit extraction list; the corrected package places the codec in an existing module and passes actual packaged `WorldShaderCompiler.createIsolated` compilation. The failed startup is excluded from timing. [V2 measurement](../tmp/radiance-pipeline-prototype/guide-slot-cache-v2/cave-summary.json), [runtime integration and codec checks](../tmp/radiance-pipeline-prototype/guide-slot-cache-v2/README.md).

Ordinary nearest-hit shadow queries, allowing hardware acceptance of coverage-opaque geometry while evaluating transmission afterward, increased fill to 10.759 ms and frame time to 20.912 ms. This isolated alternative is rejected. It can require additional transparent segments before discovering a farther blocker, whereas the retained forced-non-opaque path can terminate on any certified blocker it encounters. [Measurement](../tmp/ordinary-query-prototype/cave-summary.json), [coverage, OMM and transport audit](../tmp/ordinary-query-prototype/README.md).

Build-only direct HitObject extraction without reordering records build 4.474 ms and frame 20.543 ms, only 0.42% below the matched frame baseline. It remains unpromoted: that bounded gain does not yet justify adding a hardware-specific build path. [Measurement](../tmp/direct-initial-no-reorder/build-only/cave-summary.json).

Initial-fill direct extraction without reordering also shows no useful gain: fill 10.500 ms and frame 20.668 ms. Its reported stack remains 192 bytes for fill. This variant is rejected, and the original narrowed initial retrace is retained. [Measurement](../tmp/direct-initial-no-reorder/initial-fill/cave-summary.json).

The certified hardware-blocker probe V2 remains unpromoted. Disabling OMM alone records 20.855 ms start-to-start (47.950 FPS), with build 4.626 ms and fill 10.509 ms. Marking only certified blockers BLAS-opaque and adding a cull-non-opaque, accept-first-hit query records 21.722 ms (46.036 FPS), build 5.048 ms and fill 10.683 ms. RR also rises from 4.005 to 4.214 ms, so this pair includes system/clock variation; it establishes no useful gain. A probe miss retains the original ordered query. Both runs verified 3840×2054, RR Performance, four bounces, and OMM disabled. V2 removes the unnecessary skip-procedural flag; all 11 packaged native stages and SPIR-V/ABI checks passed. No optical pass or production promotion is claimed. [Paired cadence, clocks and populations](../tmp/blocker-probe-prototype/v2/cadence-comparison.json), [prototype contract](../tmp/blocker-probe-prototype/v2/README.md).

The RTXPT-inspired deferred cross-proposal MIS architecture also remains unpromoted. It samples and selects candidates using the generating distribution's PDF, then reconstructs the local/global mixture only for a winner with nonzero visibility. The required source/mixture correction preserves the finite-model estimator and the existing forward/reverse MIS. Independent enumeration covers 56 cases and 5,337 candidate tuples, but synthetic variance can increase 14.68×. Real cave fill remains essentially unchanged, 10.385 to 10.392 ms; cadence improves only 0.19%, from 50.149 to 50.243 renderer FPS, with fewer captured entities/particles and higher clocks. No meaningful speed gain is established, so optical testing and promotion were not pursued. All 44 packaged native modules and their comparison-only normalized copies validate; shared layouts and trace routing are unchanged. [Matched measurement](../tmp/nee-late-mis/CADENCE_COMPARISON.md), [estimator review](../tmp/nee-late-mis/INDEPENDENT_REVIEW.md), [frozen prototype](../tmp/nee-late-mis/README.md).

Terrain already uses static BLAS with fast-trace builds and compaction before publication; it is not unnecessarily built with ALLOW_UPDATE. Compatible refits are used for dynamic entities. The read-only RTXPT comparison found no missing terrain build-flag optimization. [Acceleration-structure audit](../tmp/queue-maximal-gpuav/STATIC_BLAS_AUDIT.md).

## CPU evidence and limits

Reusing immutable entity triangles when material identity, coverage and emission bits are unchanged reduced entity-capture mean from 2.577 to 1.695 ms and P99 from 3.394 to 2.368 ms in the bounded comparison. Whole-frame median allocated bytes fell from 783,976 to 582,104; terrain and geometry variation remain confounders.

The compact candidate's existing render-thread stage union has mean 2.631 ms and P99 3.633 ms. Its whole-frame allocation median is 469,152 bytes. **These scopes do not cover all mod-added host work and do not certify the 10 ms all-mod P99 target.** Whole-frame allocation includes work outside those scopes. Windows thread CPU time is quantized in approximately 15.625 ms steps on this setup and cannot support a precise per-frame 10 ms P99 claim.

A separate HostLoop/HostWork candidate measures outer mod callbacks, including work outside the renderer profile and telemetry finalization. It launched successfully and recorded 903 interior cave loops with occupied-work mean 2.455 ms and P99 3.036 ms; all observed CpuStage intervals were contained by HostWork. [Host baseline](../tmp/gpu-investigation/cave-host-blocker-host.json). Its [coverage inventory](../tmp/host-telemetry/COVERAGE.md) identifies uninstrumented selectors and shared host submission costs, so complete cost accounting remains required.

The engine pool-reuse comparison recorded mean 2.417 ms and P99 3.105 ms over 906 loops. Median observed allocations were 471,800 versus 474,968 bytes. However, entities changed from 99 to 97, particles from three to five, and model quads from 5,034 to 4,968. The allocation reduction comes predominantly from world capture, not the composite callback. **This pair does not establish a pooling CPU or allocation gain.** [Comparison audit](../tmp/gpu-investigation/cave-host-pool-comparison-audit.json). The pool-lifetime controls and bounded GPU-AV survival are documented separately in the crash investigation.

Allocation sampling identifies terrain-window observation at approximately 131.6 KB per runtime tick and entity capture at approximately 302 KB per host loop in the baseline; telemetry finalization contributes 4,496 bytes per loop. [Allocation and coverage audit](../tmp/gpu-investigation/cave-host-blocker-offline-audit.json). Scratch reuse and completion of the remaining observation boundaries are the next CPU work items.

Retaining the terrain observation scratch list reduces that scope's observed median allocation to 31,504 bytes per call while preserving an owned snapshot for the worker. The expanded v2 telemetry records 905 complete windows: occupied P99 3.012 ms, 3.101 ms including callback sums and shared submission packing, and median disjoint HostWork allocation 355,608 bytes. Entity workloads differ, so the whole-window reduction is not solely attributable to scratch reuse. [V2 analysis](../tmp/gpu-investigation/cave-cpu-coverage-host-v2-fixed.json), [allocation audit](../tmp/gpu-investigation/cave-cpu-coverage-allocation-audit.json). Full pre/post-wait host submission cleanup remains to be measured before certifying the all-mod target.

**The combined v3/SER/pooled-engine recording passes the steady-cave CPU target.** Across 906 complete windows, the conservative bound including all inventoried active mod callbacks and both host submission CPU segments has mean 2.297 ms and P99 **2.974 ms**, below 10 ms. Every window has its paired pre/post-await segments and complete callback totals; no observed CpuStage time or allocation lies outside the measured callbacks. The whole shared host CPU segments and any overlap widen the bound, avoiding an unmeasurable subtraction of vanilla/driver cost. The explicit GPU-completion await is excluded. [Full analysis](../tmp/gpu-investigation/cave-v3-ser-pool-host.json), [coverage audit](../tmp/gpu-investigation/cave-v3-ser-pool-coverage-audit.json).

Disjoint HostWork median allocation is 291,000 bytes/window; adding host submission and callback deltas gives a conservative 291,848-byte median and 346,696-byte P99. Terrain observation remains 31,504 bytes/call. The recording contains 86 entities, five particles and 730 cuboids; differing entity populations and tick frequency prevent assigning the whole-window improvement to any one change. This target assessment covers the specified steady cave with Reflex/FG off, not startup, interactive debug captures, reloads or all possible worlds. [Coverage inventory](../tmp/host-telemetry/COVERAGE.md).

The integrated query renderer with native-result/crash-history diagnostics also passes: **4.494 ms P99**, maximum 9.719 ms, over 904 complete windows. All diagnostic success-path work is inside the measured callbacks or pre/post-await submission segments, with no coverage gaps. Median host-inclusive allocation is 319,288 bytes and P99 1.670 MB. This run contains active terrain snapshot/copy work despite zero outstanding renderer builds at the sampled status boundaries; world capture accounts for the larger allocation tail. It is not an isolated diagnostics-overhead comparison. GPU cadence remains 47.98 frames/s, with mean instrumented GPU span 20.40 ms. [Integrated measurement](../tmp/shadow-query-diagnostics/MEASUREMENT.md), [raw-derived CPU bounds](../tmp/shadow-query-diagnostics/host.json), [GPU and cadence](../tmp/shadow-query-diagnostics/gpu-cadence.json).

## Fresh integrated queue/MAX + ownership result

The parent completed the ordinary **21:04:49.699–21:05:25.484 JST** measurement with the original173 retained dependencies plus the tested ProgramSession overlay, pooled engine and CPUv3/crash diagnostics. JFR `e01b2110-d359-40e2-90ea-47aa40556239`, PID58596. The [full measurement](../tmp/integrated-retained-20260912/runs/cave-20260912-210317-617/measurement-20260912-210427/MEASUREMENT.md) and [bounded summary](../tmp/integrated-retained-20260912/runs/cave-20260912-210317-617/measurement-20260912-210427/summary.json) preserve provenance and raw-export hashes. No validation or native watcher was used. Camera/settings match across the interval: **3840×2054, RR Performance, four bounces, FG/Reflex off**, frozen world time4,477,367.

**The current combined runtime passes the steady-cave CPU target: 3.327460 ms P99**, mean2.719618 ms and maximum4.682900 ms for the conservative host-inclusive bound. All **1,804 complete next-start windows** have complete callback totals and successful paired pre/post-await submit segments. Every one of **1,738,682 bounded CpuStage events** is contained by HostWork; there are no allocation gaps or partial submission pairs. Shared host CPU and overlap widen this bound; the explicit await and vanilla-inclusive HostLoop are not added. The existing steady-workload/observer-coverage limits remain.

The analyzer CLI has no start/end frame-bound option, so the audit explicitly selects complete host windows inside the saved measurement timestamps and matches GPU/counters by their telemetry frame IDs **3359–5162**. Debug composite counters are a different sequence. The query warmup, padding and boundary loops are excluded from these statistics, while all raw events and interior outliers are preserved. Full JFR/export counts match. All23 counter labels occur exactly once per selected frame: **120 entities,5 particles,988 cuboids (756 specialized/232 generic),872 baked quads**, zero block entities and zero terrain snapshots/copies. There are715 runtime ticks/mesh-only updates. These populations differ from earlier CPU runs and prevent attributing the allocation difference to the ownership correction.

Host-inclusive allocation is median**475,656 bytes**, P99**530,544 bytes** per window; world.capture averages450,525 bytes/call. Runtime.tick remains31,504 bytes/call. JVM sampling separately identifies112.6 MB of render-thread allocation weight in CpuStageEvent creation during recording, so the measured allocation includes observation overhead. A clear off-render-thread follow-up is scene preparation:5.711 GB sampled allocation weight, led by temporary instance-table Input objects (2.627 GB) and record arrays (918.8 MB). Independent thread counters show5.062 GB over31.64 seconds. The inspected source rematerializes per-instance inputs and a capacity-sized table on preparation. Avoiding temporary materialization or sharing unchanged immutable pages merits a separate candidate preserving revision ownership; no implementation change or performance gain is claimed here.

**GPU cadence remains50.460565 FPS**, mean**19.817455 ms**, P9920.863787 ms; all1,804 intervals exceed16.667 ms. Build/fill/RR means are**3.877101 /10.309819 /3.948009 ms**. All seven GPU stage observations are present per selected frame; the instrumented union/span means are19.497534/19.547660 ms, without double-counting nested world stages. The mean60-FPS gap is approximately**3.151 ms/frame**. All36 bounded clock samples show P0/99% utilization, graphics2895–2932 MHz and memory16001 MHz. This is a fresh absolute combined assessment, not a paired ownership-fix speedup claim. Raw CPU/JVM events, clock samples, excluded intervals and complete per-window results remain available in the linked artifacts.

## Measured direct instance input allocation promotion

The actual frozen candidate was measured before promotion (JFR `506b813d-b23c-4b24-8fb2-b345979f468a`, PID 36748). The same saved-start/end method selects 1,804 fully bounded host windows; query warmup/padding remain excluded. Scene-worker ThreadAllocationStatistics fell from **159.975 to 65.982 MB/s** (58.75% observed reduction). Input weighted samples fell from **2.627 GB / 3,847 samples to no observed samples**; the intentionally retained InstanceRecord[] weight remained comparable (918.8 → 934.1 MB). Zero samples do not imply zero changed/new-instance allocation.

Render-thread host-inclusive P99 was **3.072443 ms**, with all submission segments/callbacks, zero allocation gaps and all 1,558,280 bounded CpuStage events contained in HostWork. Median/P99 host-inclusive allocation was 354,640/409,848 bytes. GPU cadence stayed effectively unchanged at **19.797018 ms / 50.5127 FPS** (baseline 19.817455 ms / 50.4606 FPS); the GPU target remains unmet.

**Comparison limit:** entity populations changed from 120 to 105, cuboids 988 to 873 and baked quads 872 to 642; do not attribute the lower render-thread figures or the entire worker-rate difference solely to this change. Each recording has stable camera/settings/world time internally, and settings match between recordings. Mesh-only updates are comparable (715 vs 714); Input disappearance, similar slot-array cost, the direct allocation model and independent ownership/ABI audit support this narrow promotion.

Promoted only RtInstanceTablePlan, RtRetainedSceneBackend and four added tests after actual measurement and Descartes review. Engine/renderer/runtime checks and normal renderer JAR build passed: 93 engine tests reused up-to-date, 217 renderer tests, 73 runtime tests and 18 ABI executions. All **44 normal class-family files are byte-identical to the measured overlay**. Normal Java/JAR outputs are refreshed; frozen artifacts are unchanged. No paged-array rewrite or shader change belongs to this promotion.

Full populations, event counts, raw hashes, allocation estimators, clocks, stages, GC and promotion provenance: `tmp/instance-table-direct-input/runs/cave-20260912-214247-734/measurement-20260912-214359/MEASUREMENT.md` and `summary.json`; normal output proof: `tmp/instance-table-direct-input-promotion/verification.json`.

## Optical controls

The compact candidate keeps the clear-glass guide's transmitted target at 10.496 units; reflected wall centers remain near 10.65 units. Stained glass retains the expected mixed guide albedo and residual-plane ownership. [Guide metrics](../tmp/compact-payload/glass-metrics.txt).

For clear glass, stained glass, stained-glass reflection, water and cutout leaves, 64 raw-radiance samples per case were normalized by pre-exposure. Central 32×32 ROI channel means differ from the opaque-blocker baseline by at most 1.29%, below 1.83 combined estimated standard errors. This found no resolved discrepancy in those controls; correlated samples and finite coverage limit the conclusion. See [comparison](../tmp/compact-payload/comparison.json) and [raw capture metadata](../tmp/gpu-investigation/radiance-compact.json).

The application GPU fault remains unresolved; see [crash investigation](GPU_CRASH_INVESTIGATION.md). Optical captures use the serial-frame diagnostic to bound interference from that fault. Performance captures use normal frame overlap.

Direct SER extraction repeats the same glass guide cases. Its five 64-frame raw-radiance controls differ from compact radiance intersection by at most 0.99% per channel, below 1.46 combined estimated standard errors. [Guide metrics](../tmp/ser-direct-compact/glass-metrics.txt), [optical comparison](../tmp/ser-direct-compact/optical-comparison.json).

Inline shadow queries preserve all four glass guide views. The five 64-frame radiance controls differ from the direct-SER reference by at most 2.30% per channel, below 1.66 combined estimated standard errors; water is the least precise comparison. [Guide metrics](../tmp/shadow-query-prototype/glass-metrics.txt), [radiance comparison](../tmp/shadow-query-prototype/optical-comparison.json). These query controls completed with normal frame overlap and graceful shutdown.

Queued branches preserve all four glass guide views, including transmitted background depth 10.496 and reflected target geometry. The initial 64-sample white-glass, white-reflection and cutout comparisons remain within 1.13 combined estimated standard errors. Clear glass and water received a fresh 256-sample control: differences are at most 0.28% and 0.76%, respectively, within 1.19 combined estimated standard errors. [Guide metrics](../tmp/branch-record-queue/glass-metrics.txt), [initial radiance comparison](../tmp/branch-record-queue/optical-comparison.json), [clean higher-sample comparison](../tmp/branch-record-queue/optical-comparison-clean256.json).

The intermediate `radiance-branch-queue256.json` is excluded: its preceding reference run left water in the slab, which spread before ticks were frozen on the next launch. Its screenshots show extra foreground refraction; cleanup removed 491 spilled water blocks from the copied fixture. The clean repeat restores agreement. Optical runs that end on water now replace the slab with leaves while frozen before shutdown. This is a fixture-state correction, not a shader fix; the original test map remains untouched.

The MAXIMAL compiler candidate passes the same four guide cases, including the 10.496-unit behind-glass center depth and reflected side-wall normal. Five 64-sample raw-radiance cases were checked. Clear/water agree with the clean 256-sample queue reference within 0.77% and 0.91 combined standard errors; white and white-reflection agree with the 64-sample queue reference within 0.37% and 0.60 errors. A roughly 2% cutout difference in the first 64-sample candidate prompted a 256-sample repeat; it agrees with the reference within 0.21% and 0.21 errors. No nonfinite/saturated radiance was accepted. These are bounded image checks, not bitwise equivalence of compiler floating-point transformations. [Guide metrics](../tmp/radiance-pipeline-prototype/slang-maximal/glass-metrics.txt), [transmission comparison](../tmp/radiance-pipeline-prototype/slang-maximal/optical-comparison-clean-reference.json), [five-case comparison](../tmp/radiance-pipeline-prototype/slang-maximal/optical-comparison.json), [cutout repeat](../tmp/radiance-pipeline-prototype/slang-maximal/optical-comparison-cutout256.json).

## Fresh retained queue + MAX Nsight diagnostic capture

The [fresh retained capture analysis](../tmp/final-retained-20260912/NSIGHT_MEASUREMENT.md) covers five build/fill occurrences from `cave-analysis.yaml`. Mean relative frame duration is **18.676% build / 53.091% fill**. The YAML has no absolute durations: the parent's selected-first-fill 12.94 ms is not a five-frame average, and this profiler capture does not replace ordinary cadence measurements above. [Machine summary](../tmp/final-retained-20260912/nsight-summary.json) preserves per-occurrence values and metric denominators; heuristic `FrameGain`/`RangeSpeedupFactor` estimates are not treated as recoverable or additive gains.

Across all five ranges, build/fill respectively show **7.480/9.497% L1TEX long scoreboard**, **3.776/5.311% RT-core long scoreboard**, **7.488/0.148% local/global instruction-queue throttle**, and **0.116/4.199% no-instruction stalls**. Active warps average **12.844/13.755**; L1TEX hit rate **75.404/63.655%**, L2 **75.766/82.573%**. Fill math-pipe throttle is 0.044% and RT-core MIO throttle 0.090%, while read/write VRAM bandwidth is 18.841/12.047%. These observations indicate dependency-heavy execution without establishing ALU, VRAM or RT-core issue saturation, and do not make occupancy alone proof of register limitation. Hardware range metrics can include overlapping shader/queue work.

The parent's selected-fill UI reports raygen about **74% of samples**, shader-table **#Reg 75–128 / #Warp 16–24**, and input dependencies **RT 60.57%, global 28.89%, local 4.96%**. Source hotspots include `traceShadowQuery` at `trace_transport.slang:171` **11.87%**, retained-light lines 154/145 **3.83/3.55%**, global PDF line 56 **3.01%**, and local selection line 132 **2.64%**. These are selected-fill shares, not whole-trace shares or isolated source-operation costs; MAX source correlation can move/merge instructions. RT Live State entries (80/64/160 bytes, 12/16/27 values) describe suspend/reorder sites, not inline-query material liveness or register allocation. The two visible dependency shares are 0.12/0.19%; the third is unavailable. Startup build/fill stack sizes are 320/192 bytes, also a distinct resource.

The [exported shader CSV analysis](../tmp/final-retained-20260912/fill-shader-summary.json) supplies the stronger liveness evidence: **`OpRayQueryProceedKHR` reports 113 live registers**, 46,457 direct samples and **79.788% long scoreboard** (37,067 samples). The shader's observed maximum is **126**, with high-level peak correlation in water-wave/gradient evaluation. Only **0.07695%** of SPIR-V direct samples occur at 126; sample-weighted liveness is **96.701**. A rare peak can still determine static allocation. Lowering query liveness alone may therefore leave the shader allocation/warp limit unchanged; it does not rule out scheduling/spill improvements. Rematerialization must also avoid replacing retained values with more dependent global reads. CSV parsing respects its two different headers and excludes duplicate SPIR-V source annotations; Slang and SPIR-V each independently total 294,993 direct samples. Dependency-attributed samples use a different basis and are not converted to exclusive runtime fractions.

The status snapshots confirm matching camera/settings, **3840×2054**, RR quality 0, four bounces, FG/Reflex off, active unpaused RT and zero outstanding terrain builds. World gameTime remains 4,470,976 between snapshots. The client identifies RTX 5070 Ti / driver 616.56, OMM enabled and Slang 2026.14.1 extracted from packaged MAX bundle `b73e9084e0b232441d00930048f13ece1911c63f747865cdda1d31229da9d313`. This read-only investigation launched no game and made no implementation/build changes.

## Complete scalar-SoA FILL shader export

The full imported-source export now covers raygen `main` hash **61de1857ac45415c**, selected from the second RT world pipeline. It replaces the filtered-wrapper diagnosis; the earlier `fill-shader-wrapper-only.csv` remains preserved separately. SoA has **307,337 numeric direct samples** in both independent Slang and SPIR-V views, across 1,889 sampled SPIR-V rows and 37 source files. The selected-region screenshot supplies **421,079 whole-FILL samples**; 307,337 / 421,079 = **72.99%**, consistent with the rounded 73% raygen row. The remaining selected-region samples include traversal, scheduler and other shaders. Inclusive source totals and inline annotation copies are not additional samples. The 13.12 ms Nsight selection is diagnostic, not the ordinary FILL mean. [Full comparison](../tmp/scratch-soa/FILL_SHADER_COMPARISON.md), [machine data](../tmp/scratch-soa/FILL_SHADER_COMPARISON.json), [CSV](../tmp/scratch-soa/fill-shader.csv).

| Numeric SPIR-V observation | Retained raygen | SoA raygen |
|---|---:|---:|
| Direct samples | 294,993 | 307,337 |
| Reported top-three Long Scoreboard / direct samples | 55.300% | 56.624% |
| No Instructions / direct samples | 17.872% | 16.989% |
| LG Throttle / direct samples | 0.472% | 0.476% |
| Exact query Proceed direct samples | 46,457 | 48,844 |
| Query Proceed live registers | 113 | 113 |
| Query Long Scoreboard / query direct samples | 79.788% | 81.404% |
| Maximum exported live registers | 126 | 126 |
| Sample-weighted exported liveness | 96.701 | 96.406 |

The water-evaluation peak remains 126; the pipeline table still shows #Reg 75–128 / #Warp 16–24, matching the earlier retained observation. There is no measured increase in query or maximum exported liveness to explain the FILL regression. Equal extrema do not prove identical achieved occupancy, native allocation or eligible-warp throughput. Top-three stall aggregates omit lesser reasons and use a different denominator from range-level Nsight metrics; neither these shares nor overlapping dependency attribution are recoverable milliseconds. The source query row has 49,583 samples, more than the 48,844 Proceed instruction; both are distinct from the 421,079 whole-region population.

Correlated source-file shares remain similar: retained-light code **31.318 → 31.131%**, trace transport **26.982 → 27.327%**, BSDF code **10.912 → 10.897%** of raygen samples. Endpoint/guide validity locations have smaller direct shares and lower liveness under SoA, but this does not measure all scratch traffic or downstream waits. Extra scalar/address work and weaker full-record locality after SER remain plausible contributors; AoSoA32 did not support the simple tiled-locality remedy. No instruction-cache, spill, RT throughput or register-allocation root cause is established. The retained CSV is an older same-shader capture, not the fresh ordinary JFR control; workload and sampling populations differ. One color-conversion source row also fails exact Slang/SPIR-V location reconciliation, documented in the comparison, while whole-view totals and the listed query/hotspot counts reconcile.

The next conditional architectural opportunity is a **replacement local alias table carrying local/global proposal masses**, not a redundant global alias table or another scratch tile-size sweep. Global selection is already O(1), and per-vertex context totals are already hoisted. Removing the local dependent CDF/PDF chains requires coordinated bake, forward selection, hashed cross-proposal lookup and reverse MIS changes. The [existing scope](../tmp/light-sampling-review/ASSESSMENT.md) preserves eight candidates, four bounces, light shapes/color, volumes and optics, but adds about 15.98 MB per frame set and needs a finite-uniform distribution proof and statistical optical acceptance. The 31.13% source share is not a predicted saving. Actual workload evidence is the gate below; no alias implementation was performed.

The [bounded workload design](../tmp/light-sampling-review/WORKLOAD_DESIGN.md) describes raw light-table snapshots and an optional sampled consumer census. It is not implemented; it supplies no additional measured gain.

## Rejected fill rematerialization control

The bounded fill-rematerialization prototype is **not promoted; no optical validation was performed for it**. Against fresh retained control JFR `6bbbd60a-3934-49f2-8a93-9fde58d3225e`, candidate `9711d9ab-0020-4647-ba42-554b2f7d3289` regresses exact mean renderer cadence **19.972909 → 20.241830 ms (+1.346%)**, **50.067819 → 49.402649 FPS**. Fill increases **10.446853 → 10.752401 ms (+2.925%)**; P99 cadence increases **20.884192 → 21.263477 ms**. [Paired report](../tmp/fill-rematerialize/CADENCE_COMPARISON.md), [raw-derived comparison](../tmp/fill-rematerialize/CADENCE_COMPARISON.json).

Settings/camera match at 3840×2054, RR quality 0, four bounces, FG/Reflex off, OMM/SER on and the same packaged MAX runtime. Populations differ: entities **98 → 96**, particles **6 → 30**, cuboids **835 → 819**, baked quads unchanged at 504. Candidate graphics clocks are higher (**2,897.444 → 2,926.833 MHz**) and temperature lower (**72.556 → 66.389 °C**). The semantically unchanged build stage also slows **3.815524 → 3.903681 ms**. These differences prevent assigning the entire regression to the fill rewrite, but the measured pair supplies no performance gain supporting promotion. No instruction/register saving is inferred from SSA liveness reduction, and no quality-equivalence claim is made without optics.

The independent [read-only build comparison](../tmp/final-retained-20260912/build-semantic-comparison.md) matches all 4,318 nonconstant instructions after exact constant expansion and ID renaming; the build-module byte difference is accounted for by unused constants and representation, consistent with debug-line constants surviving stripping. Candidate artifacts remain isolated and the retained queue + MAX baseline is unchanged. This rejected experiment does not establish that all rematerialization strategies fail; additional dependent reads/recomputation and unchanged register-allocation peaks remain plausible limits of this particular approach.

## Rejected in-place primary accepted-hit cache

The 64-byte in-place primary-hit cache is **rejected and not promoted; no optical validation was performed or is planned for this candidate**. It preserves direction bits and stores accepted primary intersection IDs/barycentrics in reclaimable origin/depth words, removing at most one eligible fill restart per pixel without another buffer. Offline native/ABI/bit-pattern checks and independent source review passed, but timing did not demonstrate a useful gain. [Candidate status](../tmp/primary-hit-cache/README.md), [complete comparison](../tmp/primary-hit-cache/CADENCE_COMPARISON.md), [raw-derived metrics](../tmp/primary-hit-cache/CADENCE_COMPARISON.json).

Fresh control JFR `6bbbd60a-3934-49f2-8a93-9fde58d3225e` versus candidate `f7ef757f-7939-48af-99fe-48c60b3606de`: **50.067819 → 50.001598 renderer FPS**, exact mean cadence **19.972909 → 19.999361 ms (+0.132%)**, P99 **20.884192 → 21.023335 ms**. Build/fill means change **3.815524/10.446853 → 3.846523/10.543780 ms**; fill is 0.096927 ms slower (+0.928%). Instrumented GPU union is **19.649363 → 19.692061 ms**. The comparison trims recording boundaries, retains interior outliers and counts nested build/fill/bake intervals once.

Settings/camera match at **3840×2054**, RR quality 0, four bounces, FG/Reflex off, OMM/SER enabled and the same packaged MAX runtime. Populations differ: entities **98 → 91**, particles **6 → 22**, model submissions **109 → 98**, cuboids **835 → 740**, model quads **5,010 → 4,440**, baked quads **504 → 824**. Block entities and terrain snapshot/copy/reject counters are zero; terrain outstanding builds are zero at both recording endpoints. Graphics clocks average **2,897.444 → 2,932.833 MHz (+1.221%)**, memory stays 16,001 MHz, temperature **72.556 → 63.056 °C**, power **266.751 → 255.697 W**, utilization 99% in both. These coarse clocks and differing populations prevent assigning the small timing difference exclusively to the cache; no clock-normalized gain or shader energy saving is inferred.

The isolated candidate and evidence remain preserved; the retained queue + MAX baseline is unchanged. Offline correctness checks do not establish optical equivalence, and eliminating this duplicate primary traversal did not establish a performance benefit in the measured pair. No additional measurement or optics was requested after rejection.


## Rejected complete-visibility callable boundary

The isolated full-visibility callable candidate is **rejected and not promoted; no optical validation was performed or is planned for it**. It moves the existing ordered colored-shadow query loop into a separately compiled callable, returning RGB visibility and the final RNG seed while retaining material/MIS/scatter work in the caller. The frozen overlay SHA256 is `665561d82d7336c5d3488a6d817bbceeaee2c8dfba6bacdfb296fd74858eeaa1`. Production source and the retained queue + MAX baseline remain unchanged. [Candidate status](../tmp/callable-visibility/README.md), [paired measurement](../tmp/callable-visibility/CADENCE_COMPARISON.md), [raw-derived metrics](../tmp/callable-visibility/CADENCE_COMPARISON.json).

Fresh control JFR `2443b2ca-3043-4b5c-9df8-4d84ae765ca7` versus candidate `8d7fd917-7b57-45c3-aa4a-ffca91f16352`: exact mean renderer cadence **19.751309 -> 20.191061 ms (+2.226%)**, **50.629555 -> 49.526867 FPS**, P99 **20.996928 -> 21.289600 ms**. Fill rises **10.260201 -> 10.581421 ms (+3.131%)** and build **3.890291 -> 3.966128 ms (+1.949%)**. Instrumented GPU union is **19.434186 -> 19.875813 ms**, world resources/trace **14.935717 -> 15.401349 ms**, and RR **3.913163 -> 3.922847 ms**. The 909/906 complete consecutive frame-start windows trim recording boundaries, retain interior outliers and count nested GPU stages once.

Camera, player, window and all exported settings match within and between recordings: **3840x2054 output / 1920x1027 internal**, RR quality 0, four bounces, FG/Reflex off; both startup logs enable OMM. Populations are constant within each run but differ between them: entities **108 -> 110**, particles **8 -> 2**, model submissions **116 -> 118**, cuboids **899 -> 918**, model quads **5,394 -> 5,508**, baked quads unchanged at 734. Terrain snapshot/copy/reject and block-entity counters remain zero. Coarse recording-window graphics clocks average **2,925.059 -> 2,930.000 MHz (+0.169%)**, memory remains 16,001 MHz, temperature **69.647 -> 65.333 C**, power **261.771 -> 258.881 W**, and utilization 99%. These workload differences limit attribution; no clock-normalized result is inferred. The observed pair provides no gain supporting promotion.

The full-composition native gate passes: 12 compiled stages, 46 raw/normalized SPIR-V validations and 18 existing ABI comparisons. Both fill variants contain one `OpExecuteCallableKHR` and zero inline query sites; the callee contains one query site. Actual shared data is 16 32-bit words, including uint seed. Caller `Location 0` and absent callee Location are valid: the final KHR instruction references the callable-data variable directly, so matching Location decorations are not the interface contract. [Native evidence](../tmp/callable-visibility/native-validation.json).

During recording, control queried stack sizes are build/fill **320/192 bytes**; candidate **320/320**, callable **0**, and both default pipeline stacks **320**. The later control rebuild at 20:23:03 is outside its measurement and is excluded. These numbers are distinct from physical registers, actual continuation traffic and call latency; zero callable stack does not prove inlining or free calls. The subsequent GUI inspection below confirms separate callable execution. The regression alone does not establish whether register allocation, caller preservation, scheduling or workload differences dominate, and does not rule out every possible visibility split.

### Callable Nsight execution and allocation observation

The parent inspected native capture `java_2026_09_12_20_31_50`, preserved as `tmp/callable-visibility/cave.ngfx-gputrace`. In the selected first fill, raygen `main` / `fill_stable_planes_ser.slang` (hash `0xfed2562ce4c11caf`) has **53% of samples**, **#Reg 75–128 / #Warp 16–24**. Callable `main` / `visibility_callable.slang` (hash `0x3b0712a6325f7dec`) has **19%**, **#Reg 128 / #Warp 16**. Separate callable GPU execution is confirmed, while the caller's displayed register range remains the same as the inspected baseline. The visibility shader is not a low-register stage in this capture. These observations weaken the expected allocation benefit of this split without proving the exclusive cause of the timing regression. [Detailed GUI measurement and limits](../tmp/callable-visibility/NSIGHT_MEASUREMENT.md).

The same selected fill reports traversal **15%**, scheduler **10%**, any-hit **3%** at **99 registers / 16 warps**, and closest-hit **<1%** at **99 registers / 16 warps**. These rounded sample shares apply only to the selected fill, not the whole trace, and are not exclusive runtime fractions or recoverable savings. The pipeline register fields are distinct from the baseline instruction-level query liveness measurement; no new query-PC liveness or spill measurement has yet been supplied.

Selected fill spans **5.29–18.05 ms (12.76 ms)**. The five captured frames are **24.25, 24.28, 24.44, 24.43, 23.33 ms**, totaling **120.65 ms**. These instrumented capture timings are retained only as metadata and must not replace ordinary JFR performance. The parent's direct [Shader Pipelines screenshot](../tmp/callable-visibility/shader-pipelines.jpg) is preserved; the values above transcribe the supplied GUI inspection rather than an independent image extraction. Rejection, no promotion and no optics remain unchanged.


## Final GPU decision: single-query blocker remains isolated

**The single-query certified-blocker experiment remains isolated and unpromoted.** It requires globally disabled OMM, delivers only a small measured cave gain, and has unresolved reflection-albedo equivalence. No full optical pass is claimed. Production query source and public API remain unchanged. No further GPU prototype or capture is requested. [Candidate decision](../tmp/single-query-blocker/README.md).

Fresh OMM-off control JFR `8751a8d0-c6c5-41ee-ba24-6cfd199df99f` versus candidate `2bdaaa4d-b697-4104-8870-a3f6637f986e`: **50.526424 -> 51.471808 renderer FPS**, mean cadence **19.791624 -> 19.428111 ms**, fill **10.336748 -> 9.918512 ms** (0.418236 ms / 4.046% saved), instrumented GPU union **19.461457 -> 19.097483 ms** (1.870% improvement). All seven stages were verified across 905/903 complete windows. The integrated OMM-on reference was **50.460565 FPS**. Target output/internal extents remain 3840x2054 / 1920x1027; clocks are similar, while populations differ. The result leaves **2.761444 ms** of mean cadence improvement needed for 60 FPS and does not establish performance in foliage-heavy scenes with OMM disabled. [Full timing comparison](../tmp/single-query-blocker/CADENCE_COMPARISON.md).

Earlier `sq-control`/`sq-candidate` optical batches are excluded because dropped items contaminated the copied fixtures. Only the fresh clean pair informs the optical assessment; all clean 128-capture observations remain preserved without outlier removal. The initial cutout blue shift of **-2.117%** (-2.375 individual-capture SE, -3.531 primary batch SE) did not repeat: fresh 256-capture RGB shifts are **+0.239/+0.061/+0.285%**, all within **0.50 combined SE** across the reported batch choices. Other initial case ROI means are within 1.276 primary batch SE. Descriptive pixel temporal-variance ratios and spatial arrays were generated, but these observations do not prove universal mean or variance equivalence. [Clean optical report](../tmp/single-query-blocker/OPTICAL_COMPARISON.md).

The parent visually confirmed transmission/reflection background visibility. Center normals and dominant-plane metadata remain consistent in the four clean guide pairs, but clear-reflection center diffuse/specular albedo differs substantially and its cause remains unresolved. Single-frame jitter/path/texture differences are possible explanations, not proven ones. Targeted GPU optics for blockers inside the old advance-plus-TMin skipped interval, general-scene variance equivalence, and stationary cave radiance remain unperformed or unproven. Stationary cave radiance is **not required now** because this candidate is not being promoted. Candidate artifacts and raw evidence remain available; this subsection makes no change to the separate CPU assessment.

## First async finishing run — packaging failure

The isolated v2 finishing queue reserved graphics family 0, queue index 1, in the first live core/synchronization-validation launch (PID 41392). It failed before an accepted finishing submission: Fabric loaded the original `caustica.mixins.json`, and accessing the unregistered `AsyncMinecraftAccessor` raised `IllegalClassLoadError`. The client exited itself. The 31 offline behavioral checks and compiled hook checks had not tested this live resource-registration path.

No async performance, GPU overlap, synchronization pass or optical result is claimed. The installed validation layer was loaded; zero VUID/SYNC-HAZARD text matches before this startup failure do not establish a clean rendering run. V2 stays frozen and unpromoted while a separate packaging correction is prepared. [Launch and logs](../tmp/async-finish-v2-validation/runs/v2-sync-20260913-021242-320/launch-metadata.json), [archived crash report](../tmp/async-finish-v2-validation/runs/v2-sync-20260913-021242-320/crash-report.txt), [bounded result](../tmp/async-finish-v2-validation/runs/v2-sync-20260913-021242-320/first-live-result.json).
