/**
 * Session-retained meshes and scene-targeted placements.
 *
 * <p>Meshes belong to the render session rather than to a scene; instances place them into scenes. A
 * mesh's generic parameter is the shader-data schema shared by all of its placements and shading slots.
 */
package dev.comfyfluffy.caustica.api.geometry;
